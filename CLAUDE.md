# Serenita 项目智能体开发规则

## 项目定位

Serenita 的目标不是一个预先编排好的医疗问答程序，而是一个达到 Harness Agent 水平的医疗智能体。

智能体应当能够根据用户目标、当前会话、附件、当前轮次已读取的 Skill 和 Observation，自主判断下一步行动，并通过工具完成查询、解读、写入、修正等真实操作。系统负责提供能力、证据和安全边界，不替智能体预先决定完整业务流程。

## 本地测试账号

- 账号：`admin`
- 账号名称：`serenita`
- 密码：`123456`
- 该账号仅用于本地开发环境和浏览器验收，不得用于生产环境；测试时不得擅自更新其账号、账号名称或密码。

## 全新项目原则

- 每次更新都把 Serenita 当作全新项目设计，不为尚未正式发布的旧实现保留兼容性。
- `docs/` 只保存当前执行版本的契约与参考文档，以及不参与当前执行的未来愿景；不得在当前文档中保留历史版本范围、历史实现说明或发布快照。切换当前契约时，`docs/未来愿景/` 下的未来愿景和 `docs/README.md` 中的文档管理规则必须保留。
- 除非用户明确要求，否则不得添加数据迁移、事件迁移、启动迁移、旧格式转换、兼容层、双读双写或旧协议回退。
- 不得为了兼容旧代码而保留两套架构。新实现确定后，直接以新契约为唯一实现，并删除被替代的旧分支。
- 不得在应用启动时自动扫描、改写或迁移历史数据。
- 测试应验证当前目标架构和当前契约，不得要求继续兼容已经废弃的内部行为。

## 自主智能体原则

- 采用通用的 Agent Harness 驱动模型行动；允许模型动态选择 Skill、调用工具、读取 Observation、继续推理、询问用户或结束回答。
- 任务计划是当前用户询问的一次可选、轻量思路记录。行动模型仅在确实需要整理多个关注点、未决问题或完成标准时通过 `update_plan` 创建或调整；任何用户消息都不得触发无条件前置规划。
- `update_plan` 只是普通工具，只产生标准的模型请求工具调用和工具结果；后端不得为它维护或恢复独立计划状态，不得产生计划专用事件、上下文或阶段，前端不得投影计划记录或计划卡片。
- 任务计划不是独立规划模式、用户审批、操作授权、强制状态机或成功判定。行动偏离计划、遗漏里程碑、仍有开放问题或发现计划不可行都不得成为系统错误；无法执行时应在最终回答中如实说明。
- 计划不选择或绑定初始 Skill，也不得预先锁定后续 Skill、工具或行动顺序。Skill 的选择是每一步行动决策的一部分，由模型结合当前任务、可选计划、已知上下文和最新 Observation 自主决定。
- 计划只在当前轮次创建后供后续步骤参考，不继承到下一轮次；不得额外调用规划模型、拼接规划角色提示词、使用静态复杂度路由或提供通用计划兜底。
- Skill 可以描述能力边界、领域知识、可用工具、完成结果，以及领域任务所需的编号步骤、证据前置条件、条件分支和工具调用顺序。这些内容是行动模型按需读取并结合真实 Observation 执行的领域规程，不是由 Skill 加载器或 Harness 直接运行的业务状态机；模型仍可根据执行结果重试、切换 Skill、追问用户或停止。
- 工具只提供可执行的单一能力。会话 Agent 发起的数据库写入必须经由工具 → Service → Repository 完成，但调用时机和组合顺序由智能体根据上下文自主决定。医疗报告页面的手工创建与其它传统 CRUD 经由 API → Service → Repository，不伪装成工具调用。
- 一个任务可以动态读取多个 Skill；不同 Skill 可以复用同一个工具。完整 Skill 目录和 `load_skill` 参数枚举在当前轮次内保持不变，读取后不得隐藏、移除或禁用对应 Skill，也不得用“已经读取”之类的防御性分支拒绝再次读取；当前分支可见历史中已成功读取且指令仍在上下文中的 Skill 会延续到新轮次，被压缩或不可见时需重新读取，模型根据可见历史自主决定是否以及何时再次读取。
- Observation 必须回到智能体行动循环，由模型根据真实结果决定下一步，不得由硬编码业务分支替模型作出业务判断。
- 失败时应把真实失败信息交给智能体，由智能体决定重试、换工具、询问用户或停止；不得回退到旧的静态流程。
- Provider 边界允许把同一组逻辑工具参数 Schema 适配为严格文本工具协议，以支持没有原生工具调用的文本模型；适配器只负责 wire 序列化、解析和统一工具调用转换，不得包含业务路由、业务状态机或第二套 Agent loop。

## 工具描述分层与固定术语

- 文档中的技术专用名词使用固定英文名称：Skill、Observation、Schema；正文使用“Skill 目录描述”“Skill 正文”和“工具参数 Schema”。术语表提供中文释义，医疗业务名称与普通动作继续使用中文。长期记忆文档中表达架构对象、层级和状态的关键术语采用固定英文名称，正文、图示和算法说明同步使用，术语表提供中文释义；模块名称保留中文，研究对象沿用英文原名，具体遵循[长期记忆固定术语](docs/未来愿景/长期记忆与认知架构.md#固定术语)。代码标识、接口字段及准确引用的模型提示词、工具描述和界面文案保留原文。

- 工具顶层描述只包含工具能力和高层返回效果；不得写参数名、格式、缺省值、空值行为、参数关系、领域判断或行动规程，也不得使用“原子”修饰工具或动作。
- 工具参数 Schema 负责完整说明每个参数的准确含义、标识来源、格式、缺省值、空值行为、互斥关系、条件依赖和服务端具体约束。
- Skill 目录描述只说明适用任务和能力范围；Skill 正文承载领域知识、取证要求、判断规则、步骤、分支、失败处理和完成标准。
- 系统提示词承载产品定位、全局授权与安全边界、跨场景证据原则、行动循环和统一术语，不写具体参数手册或领域分类细节。
- 文档固定使用“工具、工具参数 Schema、工具调用、工具结果、Observation、Skill、Skill 目录描述、Skill 正文、当前轮次、当前任务、最终回答”。模型提示词与工具描述中的中文用词仍遵循当前运行时契约。
- 固定使用“医疗报告、医疗报告档案库、医疗报告事实、医疗报告解读、解读结果、检验指标分类目录、写入医疗报告档案库”。
- “医疗报告解读”表示任务、能力、过程、授权和 Skill 名称；“解读结果”表示医疗报告解读生成或更新的内容，包括正文、状态、更新时间和过期状态。可以表述为“进行医疗报告解读并将解读结果写入医疗报告档案库”。
- 多个并列项共享“医疗报告”修饰语时，只在首项保留“医疗报告”，例如使用“医疗报告导入、更新与解读”，不写成“医疗报告导入、更新与医疗报告解读”；独立表示任务、Skill 或授权时仍使用“医疗报告解读”。
- 联网统一使用“搜索结果、搜索结果摘要、网页正文”。`web_search` 返回单项搜索结果及其搜索结果摘要，`web_read` 读取对应的网页正文。
- “来源”“来源元数据”“来源原文”“原始证据”定义不同，不得互相替换。来源是与医疗报告关联的可追溯原件；来源元数据描述其身份、类型、顺序和关联状态；来源原文是实际读取的原件内容；原始证据是实际用于当前医学判断的来源原文。
- 医疗报告界面动作固定使用“开始解读”和“重新解读”。
- 不得把 Tool、Turn、指标字典、检验字典、校验观测、完整解析结果、AI 解读、写库、落库、真实观测、最新观测、本轮、当前轮、最终回复、正式回复、医疗报告分析、分析正文、分析状态、解读正文、解析观测、校验工具观测、统一校验结果、候选来源、候选结果、候选片段或来源正文作为上述术语的同义表达。文档中的 Skill、Observation 和工具参数 Schema 按本节英文术语规则使用。

全部模型可见工具的顶层描述固定如下，更新工具时必须同步验证，不得自行扩写：

| 工具 | 顶层描述 |
| --- | --- |
| `load_skill` | 读取一个技能的完整技能正文并返回实际指令文本。 |
| `update_plan` | 创建或调整当前轮次的非强制任务计划，并返回计划内容。 |
| `web_search` | 搜索公开互联网，返回搜索结果摘要和引用标识。 |
| `web_read` | 读取一条既有搜索结果的网页正文，并保留其引用标识。 |
| `read_report_catalog` | 读取当前账号中匹配的医疗报告目录，返回用于定位医疗报告的目录信息。 |
| `read_report_information` | 读取匹配医疗报告的医疗报告事实。 |
| `read_report_analysis` | 读取匹配医疗报告的既有解读结果。 |
| `read_lab_dictionary` | 读取当前账号的完整检验指标分类目录。 |
| `create_report` | 将一份医疗报告写入医疗报告档案库，返回创建结果。 |
| `link_duplicate_sources` | 将来源关联到一份既有医疗报告，返回关联结果。 |
| `merge_report` | 将补充的医疗报告事实和来源合并到一份既有医疗报告，返回合并结果。 |
| `write_report_analysis` | 将解读结果写入医疗报告档案库中的目标医疗报告，返回保存结果。 |
| `update_report_fields` | 更新一份医疗报告的基础信息或结构化内容，返回更新后的医疗报告。 |
| `add_lab_report_items` | 向一份检验报告添加检验指标记录，返回更新后的医疗报告。 |
| `delete_lab_report_items` | 从一份检验报告删除检验指标记录，返回更新后的医疗报告。 |
| `reclassify_report` | 使用经过校验的新结构重分类一份医疗报告，返回重分类后的医疗报告。 |
| `delete_report` | 删除一份医疗报告及其已保存内容，返回删除结果。 |

`report-analysis`、`analysis_content`、`analysis_outdated`、`analyzed` 等代码标识和接口字段保持不变，不因中文术语调整而重命名。

## 长期记忆架构边界

- 未来长期记忆与认知架构统一遵循[Append-only 规则](docs/未来愿景/长期记忆与认知架构.md#append-only全部记忆只允许追加)。全部记忆对象、关系、状态、索引、摘要及持久 Projection 一经成功追加，其内容、标识、时间和引用不可修改或删除。
- 新资料中的事实与后续变化各自追加为独立 Event；关系、状态和派生认识通过追加相应 Record 表达。各类关系采用同一追加机制，当前及历史结果按关系、状态、明确时间和记录截点计算；访问限制独立保存，读取执行实时权限。不得覆盖旧记录、清空重建、压缩替换或裁剪记忆历史。
- 不可变历史是记忆本身的数据契约；现有业务 CRUD、业务文件与文档生命周期仍按各自契约执行。本愿景尚未纳入当前执行，修改其文档不等于修改现有运行时或业务存储。

## 医疗报告执行边界

- 医疗报告工作区只有“上传医疗报告”“开始解读”和“重新解读”进入 Harness。手工录入新医疗报告、列表、详情、类型筛选、源文件、字段编辑、手工解读结果正文编辑和删除都使用普通 HTTP API。
- 页面手工创建、字段保存和删除是用户已经明确发出的单次命令，必须走 API → Service → Repository；普通并发更新采用最后有效写入优先，不使用内容版本令牌，不得创建会话、调用工具或追加二次确认。
- 会话里目标和新值明确的医疗报告更新、元数据更新、重分类或删除，可以在当前轮次直接通过工具执行；不得创建通用变更提案。信息不明确时由 Agent 自然追问。若先发生的删除、合并或其他破坏性操作已使目标或必要依赖不存在，后续操作必须失败并返回明确提示，不得恢复已删除对象、猜测替代目标或静默改写其他对象。
- 模型根据医疗报告内容、目录和按需读取的相近医疗报告事实选择写入方式；写入工具内部校验内容和来源，通过后写入医疗报告档案库，失败时返回错误。
- 医疗报告导入写入只提供创建医疗报告、关联重复来源和无冲突合并三个工具。不同旧值、指标映射歧义或外部语料变化必须作为真实 Observation 返回，不得自动覆盖，也不得转成待决状态机。
- 医疗报告原始证据与既有解读结果必须分开读取。医疗报告解读 Skill 规定取证要求，`write_report_analysis` 负责把解读结果正文写入当前账号中存在的目标医疗报告。

## 禁止的实现方式

- 禁止在 Harness、页面或附件路由、API、Service、Repository 或专用 Python 编排分支中，为医疗报告导入、解读、查询、更新或健康会话建立替模型决策的固定工作流、固定阶段或固定工具调用顺序。Skill 可以规定编号步骤、必需取证和条件分支，但必须由行动模型读取后执行，每次 Observation 仍须回到动态行动循环。
- 禁止使用按页面、附件类型、关键词或意图写死的专用 Python 编排分支代替智能体决策。
- 禁止把完整业务状态机、下一步动作或预生成步骤伪装成“运行时上下文”注入模型。
- 主系统提示词必须用正文说明 Serenita 的产品功能，以及健康问答、附件处理、医疗报告导入、解读、查询比较、修正和删除等场景的行为原则、证据要求与授权边界。场景说明不得写成固定阶段、固定工具顺序或替代模型动态决策的业务状态机。
- 禁止通过静态能力映射、硬编码路由或特殊兜底逻辑绕过 Skill 和工具的动态行动循环。
- 禁止展示未真实发生的 Skill、工具、思考步骤或执行阶段。
- 当前轮次真实发生的上下文装配、模型输入、模型 reasoning、模型正文、文本协议原始输出、模型请求工具调用、工具参数与 Observation、错误和最终回答必须按单层时间线持久化并流式传输，不得替换为空白、通用摘要或仅展示状态文案。会话详情执行时间线不展示完整“大模型输入”、系统消息字段、Provider 顶层参数或模型结束记录；这些值仍须真实持久化，以支持上下文溯源、状态、审计与 Token 汇总。Provider 请求必须只装配和序列化一次；实际发送的 payload 经一次安全持久化处理后写入 `request/header.header.provider_payload`，它是模型输入和全部 `request/context` 的唯一内容来源。上下文装配只能用 JSON Pointer 与可选字符区间对该 payload 做完整、可追溯的语义切片，不得再从逻辑 system、messages、Skill 上下文、工具参数 Schema 或历史事件拼装第二套输入。模型按需调用只读 `load_skill({"name": ...})` 读取 Skill；该工具的输入是名称，工具结果只能是实际读到的完整 Skill 正文，Skill 本身没有调用、输入、输出、激活、完成或完结。后续模型请求实际包含这条 Observation 时，必须从同一条 `provider_payload.messages` 消息投影为普通 `tool_observation` 上下文记录，与其它历史工具调用结果使用同一展示开关；不得另设 Skill 专用输入追溯分类，也不得再次注入或复制 Skill 正文。可用能力目录统一显示为“`N 个可用 Tool`”和“`N 个可用 Skill`”，并保证工具在 Skill 之前；其中 Skill 数量始终取完整注册目录，不能随读取历史减少。API Key、登录凭证、Cookie、附件二进制等非执行展示数据不得写入会话事件。工具业务 output 在结果事件持久化与流式展示前统一经 `core/tabular_json.py` 对同构对象数组做 `$keys`/`$rows` 无损列式编码，时间线与模型请求共享同一压缩形态，模型按系统提示词中的编码说明阅读；`effects` 等控制元数据不编码，需要结构访问持久化 output 的确定性消费方（Observation 再解析、压缩保留等）必须先用 `decode_tabular_json` 还原。
- 会话标题生成属于会话索引元数据，不属于用户任务的 Agent 执行链。只向界面发送最终标题更新，不得把标题生成提示词、标题模型 thinking/reasoning 或标题模型原始输出写入会话详情事件。

## 允许的确定性边界

以下内容可以由后端确定性实现，但它们只能保护系统，不能替智能体编排业务流程：

- 账号、会话和资源权限隔离。
- 工具参数 Schema 与结构化输出校验。
- 数据库事务、幂等、版本校验、并发控制和文件完整性校验。
- 医疗安全、隐私保护、审计和高风险操作确认。
- 模型调用上限、重复调用保护、超时和资源预算。

这些边界应位于通用 Harness、工具、Service 或 Repository 中，不得演变成按具体业务写死的工作流。

## Markdown 更新规则

- 当当前轮次更新仅涉及 Markdown（`*.md`）文件时，不得运行测试或校验命令，包括 `pytest`、Skill `quick_validate.py` 和其它测试套件；也不得为该 Markdown 更新添加或更新测试文件。

## 开发时的判断标准

实现任何需求前，先问：这项代码是在提供智能体可组合的能力，还是在替智能体写死下一步？

如果是在替智能体预先决定业务步骤，就应停止并改为 Skill 正文、工具、真实 Observation 和动态行动循环。只有安全性、数据一致性和系统资源保护可以成为确定性约束。

每次只要更新了任何后端代码，完成更新和相关测试后，都必须重启当前真实开发环境正在使用的后端服务，而不是只另外启动一个临时验收进程。重启前应先定位实际的服务管理进程、数据目录和监听端口（本地默认是 `127.0.0.1:8000`，但不得在实际端口已经变化时继续写死默认值），然后通过现有守护方式使旧后端进程退出，并让更新后的代码在同一真实端口重新提供服务。

后端重启只有同时满足以下条件才算完成：旧 worker 已退出；真实监听端口由新 PID 接管；新进程启动时间晚于本次更新；使用真实端口访问至少一个后端 HTTP 端点成功。必须把新 PID、实际端口和 HTTP 验证结果告知用户。另起临时端口成功、单元测试通过、静态检查通过、模块导入成功或旧进程仍在优雅关闭中，都不能宣称真实后端已经重启。如果在途轮次阻止旧进程退出，应明确处理或说明该在途任务，不得把已拉起备用进程误报为重启完成。

## 账号、成员与健康档案权限边界

- `account_id` 是规范 UUID，统一承担账号主键、操作者身份、私有目录和内部归属；`member_id` 是医疗资料所属人的不可变成员身份。健康档案是成员数据的领域集合，不另设档案 ID。`account` 字段承载可更新、大小写不敏感且全局唯一的用户标识，用于登录、界面标识和授权接收方查找；`account_name` 承载便于人识别的账号名称。账号资料更新保持 `access_revision`，成员集合、健康档案授权和成员选择偏好变化负责推进该修订号。账号可以从零位可访问成员开始；存在可访问成员时有且只有一位默认成员，零成员时默认成员为空。默认选择属于操作者，可指向自建成员或已获健康档案访问权的成员，包括只读共享健康档案对应的成员。注册创建账号和空成员偏好，第一位新建成员或首次获授健康档案访问权的成员自动成为默认成员；健康档案所有者账号可以删除最后一位自建成员。
- 医疗报告、解读结果及原件保存在健康档案所有者账号目录，查询、判重、合并和写入严格限定当前成员。聊天、附件、执行记录、收藏与模型/联网配置属于操作者，不随健康档案共享。
- 医疗报告 HTTP API 必须使用 `/api/members/{member_id}/reports/...`。聊天创建时可以固定目标成员，也可以保持不关联成员；成员删除事务必须清空所有操作者聊天与收藏的成员关联，不保存名称快照或删除标记，聊天可作为未关联成员聊天继续使用。撤权保留关联与当前名称展示，聊天仅可查看历史。历史事件中的成员身份不改写，也不用于恢复关联或授权；客户端或模型不能切换既有聊天的归属或指定健康档案所有者账号目录。不关联成员的聊天不提供医疗报告工具，但完整 Skill 目录保持可见。
- 每次读取、写入和后续模型请求前复核授权。只读权限不得写入医疗报告档案库；受邀编辑不得管理健康档案所有者账号的设置、授权或更新已有检验分类口径。撤权后拒绝新写入，不将失败目标改挂默认成员。
- 工具名称与固定顶层描述保持本文件中的原文；描述中“当前账号”的医疗报告访问能力实际受服务端绑定的当前成员及其实时健康档案权限限制。成员作用域、有效健康档案权限和健康档案所有者账号关系进入可信上下文，Skill 正文说明相应边界。
- 成员名称统一由必填的 `member_name` 保存，不另设完整姓名或成员与操作者关系字段；病史、过敏史、用药只作后续开放占位。完整当前契约见 `docs/当前执行/领域/成员与健康档案授权.md`。
