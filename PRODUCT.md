# 产品

## 文档类型

产品

## 目标用户

Serenita 面向需要管理慢性疾病的患者、需要比较历次体检结果的人群，以及帮助家人理解医疗报告的家庭成员。用户可能正处于焦虑状态、不熟悉医学术语，或需要在桌面设备和窄屏设备上查看敏感信息。他们的主要需求是咨询健康问题、整理私人报告、理解相关证据，并始终掌握可能产生重要影响的数据变更。

## 产品目标

Serenita 是一个注重隐私并按账号隔离的人工智能健康工作区。它将会话式健康指导与报告上传、结构化提取、证据搜索、报告解读、比较以及基于 Observation 的修正结合起来。统一智能体根据用户目标和真实 Observation，动态选择 Skill 与可组合工具，而不是遵循预设的医疗流程。产品成功意味着用户能够了解系统使用了哪些信息、执行了哪些操作，能够正确理解最终回答而不会被误导为医学诊断，并且在刷新页面或任务中断后仍能恢复到相同且可信的状态。

## 品牌气质

冷静、透明，并在医学表达上保持克制。产品应当让用户感受到其能力可靠，但不过度渲染；能够提供支持，但不以取代临床医生自居。

## 应避免的设计

- 隐藏证据、工具使用情况或重要操作的不透明会话界面。
- 使用装饰性渐变、过多卡片或无必要动画的通用人工智能仪表板。
- 使用制造恐慌的医学措辞、给出确定性诊断或虚构证据。
- 默认用户接受过专业训练、信息过度密集的临床软件。
- 静默覆盖报告，或隐藏账号与数据边界的界面。

## 设计原则

1. 让执行过程可供检查，同时避免其喧宾夺主，影响用户阅读最终回答。
2. 保持用户控制权：明确的页面命令或含义清楚的会话请求，即代表用户授权执行其中所述的变更；目标或新值不明确时必须追问。每次医疗写入都必须限定在当前聊天或页面绑定的成员作用域内，并通过实时健康档案权限检查和事务完成，普通报告编辑采用最后一次有效写入优先的规则。
3. 如实呈现医学证据，明确区分报告事实与模型解读，并清楚表达不确定性。
4. 在会话、报告、收藏和设置等功能中保留用户熟悉的操作方式，并使用一致的交互语言。
5. 将页面刷新、任务中断、窄屏设备和耗时较长的模型任务视为正常使用状态，而不是特殊情况。

## 无障碍与包容性

使用具有明确语义的控件、可通过键盘操作的展开与收起方式、清晰可见的焦点状态、有实际意义的无障碍名称，以及不单纯依赖颜色传达信息的状态文字。尊重用户的减少动态效果偏好，保持易于阅读的对比度和行宽，并确保没有医学或技术背景的用户也能理解界面。响应式布局必须在文档规定的 650 像素窄屏断点下保持可用。

## 成员与健康档案授权

不可变 `account_id` 为操作者及账号级数据提供稳定边界；可更新的 `account` 承担登录、用户标识展示和授权接收方查找，`account_name` 是便于人识别的账号名称。一个账号可以从零位可访问成员开始，并逐步创建成员或获授其健康档案访问权。每位成员由不可变 `member_id` 标识，名称保存为 `member_name`；医疗资料和原件保存在健康档案所有者账号目录，并以 `member_id` 关联所属成员；聊天、附件、收藏和配置归操作者。聊天创建时固定关联一位成员，也可以保持不关联成员，报告能力随该成员的健康档案权限提供；撤权后历史会话保留关联并显示成员当前名称，仅可查看历史；删除成员后清空聊天与收藏关联，保留原内容，旧聊天按未关联成员聊天继续使用；不保存独立成员名称快照，工作区进入替代默认成员或不关联成员的新聊天。报告接口统一以 `member_id` 确定成员作用域。当前完整契约见 [成员与健康档案授权](docs/当前执行/领域/成员与健康档案授权.md)。
