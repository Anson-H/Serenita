# Serenita 全量问题修正验收

验收日期：2026-09-06。

批准计划中的 25 项修正已完成。以下按计划编号列出关闭检查点；同一根因的重复发现合并实施。工作以开始时的工作区为基线，保留已有未提交修改。

## 问题关闭清单

| 编号 | 关闭检查点 | 验证结果 |
| --- | --- | --- |
| 1 | 认证生命周期及初始加载、后台刷新乱序 | 登录生命周期与初始刷新回归通过 |
| 2 | 偏好缓存、读取和保存队列绑定账号与生命周期 | 退出取消排队保存、同账号重登重载、跨账号拒绝通过 |
| 3 | 已认证写请求与退出携带预期账号，后端先核对 Cookie 身份 | 缺失请求头、账号不匹配、幂等退出及旧 401 回归通过 |
| 4 | 页面异步结果绑定成员、会话、资源及操作序号 | A→B→A、切换会话、旧订阅及报告操作回归通过 |
| 5 | 普通附件和报告共用一个批量上传入口 | 上传失败停止后续文件，成功资源保留正确会话归属 |
| 6 | 模型和联网密钥仅主动显示时读取 | 编辑后迟到明文不会填入草稿；页面内存之外不保存明文 |
| 7 | 账号及各服务串行保存，响应按提交快照合并 | 后续编辑不被旧响应覆盖，失败保留新草稿 |
| 8 | 模型列表、四种默认用途及加载状态集中维护 | 默认值取自服务端，读取失败保留最后成功数据 |
| 9 | 默认值、模型删除和能力刷新联动附件能力 | 当前成功查询才清理不支持附件，失败保留附件 |
| 10 | 报告公共字段、列表统计和详情类型分开 | 五种报告及详情解读更新时间契约通过 |
| 11 | 成员可用性及收藏来源通过公开 Service 能力读取 | 授权、撤权、删除并发和事务内复查通过 |
| 12 | 报告编辑字段由当前 Schema 派生，前端共用编辑元数据 | 字段、存储映射、收藏快照覆盖检查通过，包含其它报告正文 |
| 13 | 解读引用依据当前轮次可信联网工具观测转换后保存 | 合法、重复、未知、代码与转义引用回归通过 |
| 14 | 报告操作、预览、批量选择和上传绑定完整作用域 | 同报告 ID 跨成员隔离、旧页面结果拒绝、部分上传失败通过 |
| 15 | 领域错误与 HTTP 解耦，统一结构化错误响应 | 错误码、HTTP 语义、字段校验及敏感输入不回传检查通过 |
| 16 | 提交后文件清理由应用层组合，使用唯一公共清理入口 | 成员删除、孤立文件、幂等删除和清理失败重试通过 |
| 17 | Provider 按上游状态及异常类型分类，共用错误映射 | 认证失败、超时、上下文溢出及保存地址连接测试通过 |
| 18 | 后台执行只维护持久化事件与执行状态 | 删除无消费者 SSE 生成及回放，保留取消、失败和终态副作用 |
| 19 | 上传传输、下载地址和流响应在 HTTP 层处理 | API、附件、来源地址及 Service 分层测试通过 |
| 20 | 删除空解读缓存、备用上传、延后上传状态及无调用入口 | 报告页面和上传回归通过 |
| 21 | 删除无生产调用的模块、函数、图标及未使用声明 | 前端严格类型检查和后端静态检查通过 |
| 22 | 删除旧 JSONL 辅助实现与旧 Schema 检查器 | 测试改用真实会话持久化与当前 Schema 定义，存储回归通过 |
| 23 | 删除旧签名探测与两项无用途直接依赖 | 当前 Provider、Repository 接口回归通过，锁文件同步 |
| 24 | 会话详情与流使用同一记录投影，引用和日期能力共享 | 记录标识、排序、UTF-16 偏移、标题及重连回归通过 |
| 25 | 修正联调登录定位，前端检查包含未使用声明 | 完整联调业务断言、浏览器测试与生产构建通过 |

## 最终自动化验收

| 检查 | 结果 |
| --- | --- |
| 完整后端测试 | **1043 passed，15 subtests passed** |
| 前端单元测试 | **130 passed** |
| 前端类型与未使用声明检查 | **通过**，启用 noUnusedLocals / noUnusedParameters |
| 生产构建 | **通过**，9 个 JavaScript 分包均符合 500000 字节上限 |
| 浏览器回归 | **80 passed**，Chromium 及指定 WebKit 场景 |
| 真实 API 联调 | **1 passed**，完整业务断言已执行 |
| 后端静态检查 | **通过**，F401 / F821 / F822 / F841 |

联调使用真实 HTTP、隔离数据库、浏览器和 Harness，供应商使用测试适配器。真实供应商验收结果见下一节。业务回归涵盖成员授权与撤权、报告与收藏、附件、动态工具调用、取消、排队、编辑、重新生成、分支、标题、压缩、重连和存储事务。

主要复验命令：

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q -p no:cacheprovider
cd frontend
npm test
npm run build
npm run test:browser
npm run test:integration
```

## 真实开发服务验收

通过原有 launchd 管理方式重启真实后端；后端继续在 127.0.0.1:8000 提供服务，前端 127.0.0.1:5173 返回 200。

| 实际检查 | 结果 |
| --- | --- |
| 原本地账号 admin / serenita 登录及配置读取 | 通过 |
| aliyun_bailian 连接及模型调用 | 通过 |
| deepseek 连接及 deepseek-v4-flash 调用 | 通过 |
| openrouter 连接及 tencent/hy4-preview 调用 | 通过 |
| chat / compact / title / vision_parse 四种默认用途 | 均为 aliyun_bailian:qwen3.8-flash，实际调用通过 |
| 视觉默认模型读取真实测试图片 | 通过 |
| Exa 连接与实际搜索 | 通过，联网启用，默认服务仍为 exa |
| Tavily | 未配置，未虚构凭证或计入已通过服务 |

真实环境验收只进行登录、配置读取和外部模型、联网调用；业务测试使用隔离数据。现有账号身份、模型与联网配置、密钥及默认模型继续可用，未执行数据库或 JSONL 迁移。

## 当前契约与主要实现

- [HTTP 接口](/Users/anson/Documents/Employment/Serenita/docs/当前执行/接口/HTTP接口.md)
- [前后端职责边界](/Users/anson/Documents/Employment/Serenita/docs/当前执行/接口/前后端职责边界.md)
- [前端行为与交互](/Users/anson/Documents/Employment/Serenita/docs/当前执行/前端/行为与交互.md)
- [医疗报告契约](/Users/anson/Documents/Employment/Serenita/docs/当前执行/领域/医疗报告.md)
- [会话事件与投影](/Users/anson/Documents/Employment/Serenita/docs/当前执行/架构/会话事件与投影.md)
- [认证生命周期](/Users/anson/Documents/Employment/Serenita/frontend/src/api/authLifecycle.ts)
- [报告字段 API 与页面联调](/Users/anson/Documents/Employment/Serenita/frontend/tests/integration/boundaries.spec.ts)
- [请求归属回归](/Users/anson/Documents/Employment/Serenita/frontend/tests/request-ownership.test.mjs)

