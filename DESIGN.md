# Serenita 设计语言

> 当前执行 / 全局视觉契约。页面结构与业务交互见 [前端行为文档](docs/当前执行/前端/行为与交互.md)。

## 1. 职责与层级

本文件定义跨页面复用的视觉角色及其组合规则。产品定位见 [PRODUCT.md](PRODUCT.md)，业务字段顺序、入口名称、保存策略和接口见对应功能契约。实现数值以 [tokens.css](frontend/src/styles/tokens.css) 为准，共享外观由 [design-language.css](frontend/src/styles/design-language.css) 实现，状态组合由 [interaction-states.css](frontend/src/styles/interaction-states.css) 管理。

### 信息层级

| 层级 | 职责 | 例子 |
| --- | --- | --- |
| 工作区 | 承载当前任务或对象，包含标题栏与内容区 | 医疗报告详情 |
| 内容分组 | 分组标题与其管理的相关内容 | “基础信息”及其下方内容 |
| 内容容器 | 按内容关系选择分组列表、数据表、正文或面板 | 基础信息使用分组列表 |
| 条目 | 分组列表中的一个内容单位，可采用字段、导航、选择或操作布局 | “就诊时间”标签与其值 |
| 控件 | 条目内或独立使用的输入、选择、按钮、复选框、开关 | 就诊时间输入控件 |

这是信息组织关系，不要求每一层都增加 DOM 包装。分组标题位于内容容器外；标签、值和控件属于条目内部。术语按此层级使用，不把条目的布局再命名为另一种容器。

覆盖层是显示在页面流之上的层级，不是与分组列表并列的容器。选择菜单、上下文菜单、内容弹窗和媒体预览位于覆盖层，内部继续复用相同的容器、条目和控件规则。

### 设计取向

患者和家属可能在焦虑状态下长时间阅读与核对医疗报告，界面采用温和、适量明快的莫兰迪配色。外观由明暗主题与色彩风格共同决定，默认跟随系统并使用暖杏。浅色主要文字为纯黑、描述性文字为深灰；深色主要文字为纯白、描述性文字为浅灰。色彩用于组织表面与突出操作，状态同时通过图标和文字表达。优先用栏位、对齐、留白和分隔线组织信息，不使用嵌套卡片、装饰性渐变或无必要动画。持续浏览和编辑在原位置推进，技术细节按需展开。

## 2. 视觉基础

### 颜色

外观包含两个独立维度：明暗主题为浅色、深色或跟随系统；色彩风格为暖杏、鼠尾草、雾蓝、藕紫、燕麦。每种风格均有独立校准的浅色与深色取值，共十种组合。切换明暗保留色彩风格。入口与偏好保存见[主题设置](docs/当前执行/前端/行为与交互.md#主题)。

| 色彩风格 | 浅色表面与强调 | 深色表面与强调 |
| --- | --- | --- |
| 暖杏 | 暖白、杏米与柔和杏桃 | 暖炭灰与灰陶杏 |
| 鼠尾草 | 灰白、浅灰绿与草木绿 | 绿调炭灰与沉稳灰绿 |
| 雾蓝 | 灰白、浅蓝灰与雾蓝 | 蓝调石墨灰与深蓝灰 |
| 藕紫 | 暖白、浅藕灰与柔和灰紫 | 紫调炭灰与深藕灰 |
| 燕麦 | 米白、燕麦灰与沙色 | 暖石墨灰与深灰褐 |

全部颜色 Token 的名称、数量与职责固定；两套明暗主题和五种风格只覆盖现有取值，不增加颜色 Token、别名或页面专用色值。基色使用 OKLCH，派生表面由已有 Token 混合生成。准确数值仅由 [tokens.css](frontend/src/styles/tokens.css) 维护。以下是完整颜色角色目录：

| Token | 用途 |
| --- | --- |
| `--color-surface` | 浏览器画布、应用主内容、菜单与内容弹窗 |
| `--color-rail` | 导航侧栏、会话输入区域与账号头像 |
| `--color-header-surface` | 折叠标题与表格表头 |
| `--color-control-surface` | 分组列表、标准与紧凑控件 |
| `--color-content-inset` | 附件、代码、引用块、资源标签与预览衬底 |
| `--color-foreground` | 所有主要文字和普通图标：浅色纯黑，深色纯白 |
| `--color-foreground-muted` | 描述、元信息与次要说明：浅色中性深灰，深色中性浅灰；同时用于次要控件轮廓与轨道 |
| `--color-divider` | 分隔线、必要边框与图表辅助线 |
| `--color-danger` | 危险与失败的图标、轮廓和标记 |
| `--color-danger-soft` | 危险控件、错误通知与异常提示的辅助表面 |
| `--color-accent` | 焦点轮廓、开关、复选框与强调标记 |
| `--color-primary-surface` | 主要操作与品牌标记的表面 |
| `--color-primary-hover` | 主要操作的悬停表面 |
| `--color-item-hover` | 普通条目与控件的悬停表面 |
| `--color-item-current` | 当前对象、单选、编辑与展开状态的表面 |
| `--color-danger-hover` | 危险操作的悬停表面 |
| `--color-warning` | 注意与核对提示的图标、轮廓和标记 |
| `--color-warning-soft` | 注意与核对提示的辅助表面 |
| `--color-success` | 成功与已确认结果的图标和标记 |
| `--color-surface-error` | 会话错误表面的混合基色 |
| `--color-surface-raised` | 模式选择、执行记录与通知的补充表面 |
| `--color-inverse-surface` | 覆盖层暗色遮罩的基色与深色主题代码区底色 |
| `--color-inverse-text` | 深色主题代码区文字，使用纯白 |
| `--color-inverse-divider` | 深色主题代码区边框 |
| `--color-scrollbar-thumb` | 滚动条滑块 |
| `--color-scrollbar-thumb-hover` | 滚动条滑块悬停 |
| `--color-upload-progress` | 上传进度的已完成部分 |
| `--focus-outline` | 引用强调色的 2px 实线键盘焦点轮廓 |
| `--shadow-overlay` | 适配明暗主题的覆盖层阴影 |

页面背景的饱和度最低；侧栏、标题、控件和内容衬底通过明度区分。主要按钮集中使用风格色，浅色按钮底色足够明亮以承载黑字，深色按钮底色足够深以承载白字。强调标记与按钮底色分别校准，确保开关、勾选与焦点在所在表面清楚可见。

所有文字使用主要或次要前景，包括按钮、标签、链接、代码和危险操作说明。链接保留可辨认的下划线。危险、警告与成功使用跨风格一致的柔红、赭黄与灰绿色，按明暗分别调色；语义色只用于状态图标、轮廓和表面，并同时提供文字或可访问名称。风格色接近状态色时，通过固定图标、文案和状态表面辨认含义。图表复用现有颜色，多个系列辅以实线与虚线，睡眠阶段同时使用行位与文字图例。

悬停不能覆盖选中、编辑与展开状态；多选的持续状态只由复选框表达。键盘焦点使用 `--focus-outline`，不改变已有底色。禁用与运行状态沿用原有透明度规则，不另设状态色。普通文字对实际背景的对比度至少为 4.5:1，必要控件标识与焦点轮廓至少为 3:1。含透明度和混合色的表面以最终合成颜色验收。

`:root` 和 `body` 使用主表面作为画布背景；页面首次绘制前应用保存的主题并同步 `color-scheme`，原生控件采用相同明暗。顶层菜单、弹窗、文件预览和悬浮模糊层继承同一主题。暗色遮罩由反色表面混合生成，深色主题下仍压暗底层。浅色代码区使用内容衬底和黑字，深色代码区使用反色表面和白字；原件图片与文档保留原始内容。

### 文字

界面使用 `--font-ui`，代码使用 `--font-mono`。连续正文限制在 `65–75ch`，结构化字段与数据使用容器可用宽度。

| 文字角色 | 字号 / 行高 | 字重 |
| --- | --- | --- |
| 元信息、说明文字与紧凑控件 | 13px / 18px | 常规 500 |
| 正文、条目、字段与标准控件 | 15px / 20px | 常规 500 |
| 分组标题 | 16px / 21px | 加粗 700 |
| 页面与内容弹窗标题 | 18px / 24px | 加粗 700 |
| 品牌名 | 22px / 30px | 加粗 700 |
| 指标摘要数值 | 24px / 30px | 加粗 700 |
| 指标详情主数值 | 36px / 44px | 加粗 700 |

标题、标签和说明文字各自提供新信息，不复述。说明文字限于对象摘要、通知详情、隐私、安全、不可逆后果、错误、空状态或容易误解的技术含义。

### 间距与尺度

| 角色 | Token | 数值与用途 |
| --- | --- | --- |
| 网页最小宽度 | `--size-page-min-width` | 300px，页面布局维护的最小宽度 |
| 触控目标 | `--size-touch-target` | 44px，仅用于粗指针触屏环境的实际可点击范围 |
| 标题栏 | `--workspace-titlebar-height` | 50px |
| 标题栏按钮 | `--size-titlebar-control` | 30px，纯图标时等宽 |
| 标准条目、标准控件、标准条栏 | `--size-control-primary` | 40px，富内容与多行控件以此为最小高度 |
| 紧凑条目、紧凑控件、紧凑条栏 | `--size-compact-bar` | 30px，用于紧凑的单项信息、单项操作与横向状态栏 |
| 标准内嵌按钮 | `--size-inline-button` | 20px，纯图标时等宽 |
| 紧凑内嵌按钮 | `--size-inline-button-compact` | 18px，纯图标时等宽 |
| 内容间距 | `--space-content` | 15px，内容区内边距、内容分组之间、覆盖层安全距离 |
| 控件水平间距 | `--space-control-inline` | 10px，所有条目、控件与条栏的水平内边距 |
| 标准控件垂直间距 | `--space-control-block` | 10px，标准条目、控件与条栏的垂直内边距 |
| 紧凑控件垂直间距 | `--space-control-block-compact` | 6px，紧凑条目与控件的垂直内边距 |
| 关联间距 | `--space-related` | 5px，标题与内容、内嵌操作内部 |
| 条目间距 | `--space-item` | 2px，同层连续条目；分组列表内部由分隔线组织，不留条目间距 |
| 复选框 | `--size-selection-control` | 16 × 16px，内部对勾 12 × 12px |
| 开关 | `--size-switch-track-width/height` | 32 × 16px，滑块 12 × 12px |

`inline` 表示水平方向，`block` 表示垂直方向。内边距简写按“垂直、水平”引用，`gap` 按“行间距、列间距”分别引用垂直与水平 Token。紧凑条目、控件与条栏统一由共享控件样式管理：内部行高 18px，上下各 6px 内边距，单行总高度 30px；高度 Token 由行高与内边距计算，多行内容自然增高，页面不得另设固定高度或重复叠加内边距。紧凑文字按钮使用 `control--compact`，附件、注释、引用、状态与执行记录条栏使用 `compact-control-bar`。

同一容器只有一层内边距。标准条栏与紧凑条栏都是横向布局，内嵌操作是使用位置，图标按钮是内容形式。标准内嵌按钮用于标准条栏、正文操作和输入辅助操作；紧凑内嵌按钮用于标签、附件和注释等密集内容。标准文字按钮、标题栏按钮、紧凑控件、标准内嵌按钮与紧凑内嵌按钮通过所属尺寸组合外观，不因页面改变。

复合条栏直接组合既有条栏和控件样式：容器使用 `data-row-surface`，主要操作使用 `data-interaction-owner="row"`，附属操作使用 `data-interaction-owner="self"`。悬停、展开与键盘焦点由现有共享规则处理，主要操作禁用时整条撤销悬停，附属操作按自身状态继续可用。条栏承担内边距，内部主要控件不重复增加内边距。附件、注释与医疗报告引用遵守同一规则；纯展示条栏不声明交互表面。

标准条栏由共享样式统一提供上下与左右各 10px 内边距，20px 标准内嵌按钮与 20px 单行内容共同形成 40px 单行高度；40px 为最小高度，多行内容自然增高。页面不得重复声明条栏内边距或固定高度。

粗指针触屏环境将标题栏、标准、紧凑和内嵌控件的实际目标统一提升到至少 44 × 44px；文字和图标尺寸、复选框方框及开关轨道的视觉尺寸保持不变。尺寸扩展通过控件自身留白和列表行高实现，不使用会与相邻控件重叠的透明伪元素。桌面与精细指针环境继续使用上表的紧凑视觉密度。

全部对象列表、关联列表与明细列表的多选状态统一复用 `ListSelectionBar`。左侧显示加粗计数，右侧保留“全选”“退出”图标按钮，不显示文字；状态栏采用标准尺寸，计数使用 15px 正文字号，图标按钮使用 40 × 40px 标准尺寸，保留悬停提示与完整可访问名称，全部选中时更新为“取消全选”的提示与状态。状态栏背景透明，图标之间使用关联间距，窄栏允许自然换行，触屏点击区域遵循共享规则。

多选栏按所在位置复用原有操作区，每个列表只显示一处：

多选引起的布局和操作入口变化限于当前页面或列表面板；并列详情、其他页面及全局导航独立维护。详情内子列表多选的影响限于所属详情页面。

| 原有布局 | 进入多选后的布局 |
| --- | --- |
| 上方为分隔横线，例如聊天列表 | 保留横线，多选栏与上下相邻内容各保留一层 5px 外部间距；标准控件内部的文字垂直居中保持不变 |
| 列表第一项为“添加”“创建”等入口 | 用独立多选栏替换该入口的位置；多选栏移出分组列表容器，使用透明操作栏形式，下方的数据条目继续使用原有分组列表 |
| 列表上方为搜索栏、下拉筛选栏或组合筛选区 | 用多选栏原位替换该操作区；进入多选时隐藏原控件并保留其值，退出后恢复控件及原搜索、筛选条件 |

存在搜索或筛选区时优先复用该区域；没有该区域时复用列表首项创建或添加入口的位置。同时存在的其它创建或添加入口在多选期间隐藏，不再增加多选栏。替换沿用原操作区的占位与布局，保持下方数据列表的位置、宽度、条目内容、顺序、样式与滚动位置；仅按多选语义切换复选框、进入箭头和选中状态，避免额外插入一排控件将列表向下推移。5px 外部间距适用于分隔线旁的独立多选栏；原位替换场景沿用原操作区与列表的布局间距。各处间距由一层容器承担，避免叠加容器 gap、列表内边距和状态栏外边距。退出多选后恢复原操作区及创建或添加入口。

“删除”“置顶”“收藏”“设置标签”等执行操作统一放在列表下方的 `ListBulkActions`，采用紧凑控件，使用前置图标加文字，按内容宽度靠右排列。删除按钮固定显示“删除”，不按页面改名；执行期间保留文字并禁用按钮。下方操作区与列表保留一层 15px 内容间距，滚动列表下留白和操作区上内边距不重复累加；工作区目录的下方操作区位于滚动列表之外，始终可达。详情内的子列表多选时，隐藏整个对象的删除入口，退出后恢复。

用户消息气泡的内边距遵循标准控件规格：上下使用 `--space-control-block`、左右使用 `--space-control-inline`，均为 10px，不使用内容区的 15px 间距。纯文字、包含资源引用及消息编辑状态共用此规则；边框占位不计入内边距。

会话输入框由外容器统一提供标准控件内边距，内部文字输入区域不再叠加内边距。

表面、控件与背景标记使用圆角矩形；圆角为参考高度的四分之一，细长标记以短边为参考。分组列表标准格式参考高度为 40px、紧凑格式为 30px；根级折叠列表以标准条目为参考，内容弹窗以标题栏为参考，面板使用 48px。参考高度不限制实际内容高度。拼接边圆角为零，整体外轮廓由容器裁切。

成员与账号头像共用 30px 尺寸、7.5px 圆角和标准用户图标。身份栏右侧图标按钮采用 30 × 30px 占位、22 × 22px 图标；主要区域和附属控件各自遵循共享交互归属。健康档案身份栏与功能入口之间的分隔线左右内缩 15px，上下各留 15px。四个入口横向等宽，间距为 `--space-related`，名称采用 13px / 18px 元信息规格，窄屏保持一排；触屏目标按全局触摸规则扩展。具体入口与导航行为见[前端行为](docs/当前执行/前端/行为与交互.md#成员)。

侧栏底部账号设置是单一导航按钮，显示 30 × 30px 账号头像和来自 `account_name` 的加粗账号名称；可用空间不足时显示省略号。导航悬停和当前页状态作用于整条，账号头像自身保持中性表面。

### 扩展标准控件

标准控件的描述信息有两种排列形式。主文字统一采用 15px / 20px、常规 500 和主要前景；描述统一采用 13px / 18px、常规 500 和次要前景。

- 下方描述：复用 `ControlRowContent`，描述放在主文字下方，两行左对齐，垂直间距为 `--space-related`（5px）。
- 右侧描述：主文字在左，描述复用 `control-row-description`，放在条栏右箭头的左侧；没有右箭头时放在条栏右侧。描述与主文字垂直居中，可与能力图标共同组成右侧摘要，箭头始终位于最右侧。图标复用标准 16px 规格和对应详情中的语义，提供完整的可访问说明与悬停提示。长主文字单行省略并保留完整提示，右侧摘要保持可见。

左侧图标可选，使用标准 16px 图标，与文字组垂直居中，间距为控件水平间距；省略图标时文字直接从控件左内边距起始，不保留图标占位。无描述时主文字垂直居中。两种形式的外层均复用标准控件或标准条栏的最小高度、上下及左右各 10px 内边距和 10px 圆角，内容自然增高；文字组件不重复增加内边距。

通知列表和右上角状态通知使用下方描述形式，主文字直接说明事件，例如“回答已生成”“用药时间到了”“药品过期”；描述只占一行，超长时省略并通过悬停提供完整文字，屏幕阅读器保留完整描述。通知页面的顶层标题栏铺满主区域，外层不叠加内容留白。通知内容居中，最大宽度复用会话详情的 `--size-conversation-content`（750px），窄屏两侧各保留内容间距。列表复用标准 `GroupedList`，待处理在前、已读在后，各自按发生时间从新到旧排列，取消状态切换标签。已读通知的 15px 标题使用 `--color-foreground-muted` 灰色次要前景，保留正常查看交互；回答通知的描述只显示聊天标题，例如“体检结果咨询”；用药通知只显示成员与药品，必要时附上剂量，例如“妈妈 · 阿莫西林 1 粒”，不拼接字段标签、操作说明和时间。发生时间以相对时间显示在条目右侧，与主文字同一行；标为已读使用右侧标准内嵌图标按钮，与描述同行。状态通知只显示标题和描述，不增加“成功”“提示”等第三行分类标签。通知操作使用共享控件，独立承接交互。通知名称示例不代表已接入相应通知类型。

### 身份信息条

账号、成员、健康档案基本信息和药品概要等同类信息条统一复用 IdentityRowCopy：无描述时仅显示垂直居中的名称；有描述时名称在上、说明在下，左对齐，使用正文与元信息规格及 5px 间距，图标和右侧操作随文字组垂直居中。容器使用共享 `identity-row`，`IdentityRowCopy` 复用 `ControlRowContent` 并使用加粗名称，样式统一归入 `design-language.css`。是否整行可点击由具体操作决定，附属按钮独立承接交互。

### 图标

图标集中在 `components/icons.tsx`，使用 `50 × 50` viewBox、5 单位内部描边和圆角端点、连接。

显示尺寸：标题栏按钮为 22px；标准条目、标准控件、标准条栏和标准内嵌按钮为 16px；紧凑条目、紧凑控件、紧凑条栏和紧凑内嵌按钮为 14px。

紧凑图标尺寸由共享控件样式统一管理，包含前置图标、内嵌操作和展开箭头。14px 图标在 18px 内容区内垂直居中；页面只定义排列与状态，不再单独设置紧凑图标尺寸。

管理操作区、批量操作区与表单底部操作区统一使用前置图标加文字标签，同一操作在菜单、列表和详情中复用同一图形。置顶使用图钉，收藏使用星形，添加使用加号，删除使用垃圾桶，设置标签使用标签轮廓，合并使用汇合箭头，保存与完成使用对勾。图标继承按钮文字颜色，危险操作沿用危险前景，不另设装饰色；请求期间保留图标与操作位置。空间不足时操作组换行，不缩小图标或挤压文字。日期、筛选选项和普通文本链接不强制附加图标。

关闭、移除使用 `XIcon`，退出登录使用门框与向右箭头组成的 `LogOutIcon`，下拉与折叠使用 `ChevronDownIcon`，进入与返回分别使用右、左箭头。图形本身的圆形线条不影响控件的圆角矩形外观。

## 3. 布局与容器

### 工作区、标题栏与滚动区

页面、详情与内容弹窗的标题默认继承直接入口的主名称。每进入一层，使用该层入口名称；不自行添加“填写”“详情”“管理”等前后缀，也不继续沿用更早层级的标题。入口中的图标、数量、日期、状态、当前字段值及辅助说明不属于主名称。对象详情使用与列表相同的对象名称；保存改名后，列表与详情同步显示已保存名称。

只有导航进入下一层才改变标题。选择表单字段、切换筛选、展开内容和提交状态变化不改变当前层级标题；返回时恢复上一级标题。直接链接、刷新和浏览器前进后退依据目标路由及对象数据确定同一标题，不依赖来源 DOM、上一页字符串或浏览器历史来猜标题。

纯图标或“编辑”等缺少对象语义的入口使用明确的动作名称；根工作区、尚未载入对象的占位、确认窗口及图表快捷入口的例外逐项列在[页面标题对应表](docs/当前执行/前端/行为与交互.md#入口与标题)。不得以“特殊页面”为由自行扩大例外。正文中仅重复标题的说明删除，必要业务字段保留。

静态入口与目标标题共用 `navigationLabels`，对象名称共用对应领域的显示函数或名称字段。`WorkspaceToolbar`、`DialogTitlebar` 和设置标题栏复用 `NavigationTitle`，标题完整文本保留在 DOM 和可访问名称中；空间不足时单行省略，悬停可查看完整名称，左右按钮不被长标题挤压。

标题栏使用等宽左右操作区与居中标题，下缘以分隔线连接内容区。健康档案与收藏未打开详情时，集合栏占满工具区，标题栏铺满整区，内容居中并复用 `--size-conversation-content`（750px）的最大内容宽度，两侧保留内容间距；打开详情后，桌面划分左右栏位，再复用标题栏，窄屏展示当前详情。返回表示产品内的信息层级，无法返回时不渲染返回控件。健康档案根级不显示返回控件，也不在标题栏重复显示切换成员控件；成员切换和添加统一位于侧栏身份栏。

内容分组之间使用内容间距。标准条栏内的操作与状态垂直居中，使用关联间距；附属于正文的操作组不额外套用条栏高度。

设置、医疗报告、收藏、个人信息、健康日记、用药与身体指标详情统一使用 `--size-content-column`（750px）的最大内容宽度，可用空间不足时铺满，正文两侧至少保留 15px。通知列表沿用同一内容列。子页复用所在容器的标题栏和滚动区，不叠加正文内边距。用户消息气泡在各屏幕宽度下均以正文列的 88% 为最大宽度。

全项目使用一套滚动条。滚动区保持左右总留白相等：右侧内容留白加滚动条实际占位等于左侧留白，浮动滚动条按实际 0px 处理。正文滚动区使用 `scroll-content`；菜单、引用预览、待发送列表与执行记录等局部滚动区使用 `scroll-balanced`，均按自身实际占位计算，不继承其它容器的占位。固定操作区与滚动正文保持同列对齐，底部空间随固定区域高度调整，不遮挡正文。聊天列表的“发起新聊天”和医疗报告列表的“创建医疗报告”使用与会话输入框相同的底部覆盖关系，悬浮在各自滚动区之上，不随列表滚动；按钮与列表内容同列对齐，滚动区末尾保留至少一个内容间距。

### 悬浮按钮与输入区域

所有覆盖在滚动内容之上的悬浮按钮、操作组和输入区域，背后统一增加高斯模糊。模糊层位于内容与控件之间，只模糊背景，按钮、文字、图标和输入内容保持清晰。同一栏位同一边缘的悬浮控件共用一层，横向覆盖所属栏位的完整宽度，左右到达栏位边界并保持完整材质，不做左右渐隐。材质范围由所属栏位决定，按钮或输入区域的宽度不限制材质宽度。靠页面边缘的一侧覆盖到所属区域边缘，包含安全区和输入区随可视视口移动产生的底部留白，不能在控件下缘截断。此规则统一适用于悬浮按钮、操作组和输入区域。背景层之间可以重叠，所有可操作控件保持在这些背景层之上；“回到最新”按钮作为例外，不增加背景模糊，层级高于整个输入区及其模糊层，保持按钮清晰可操作。

背景材质由高斯模糊和半透明页面底色共同组成，必须叠加所在区域的底色，以减弱下层文字模糊后形成的色带。正文区域使用主表面 `--color-surface`，聊天侧栏使用次级表面 `--color-rail`；底色不透明度使用 `--floating-backdrop-tint-opacity`（88%）。底色和模糊共用同一伪元素及渐隐遮罩，靠控件处保留完整底色浓度，仅向正文方向一起渐隐。

底部悬浮时，控件背后的模糊完整显示，并从控件上缘向上逐渐消失；顶部悬浮时方向反转，从控件下缘向下逐渐消失。方向由悬浮位置决定。渐隐用于衔接滚动正文，属于功能性遮罩。模糊层不拦截点击、滚动或选择，滚动内容继续按操作区实际高度预留空间。

材质设计参考 [Apple Materials](https://developer.apple.com/design/human-interface-guidelines/materials) 对模糊、鲜明度和混合效果的组合，以及 [Microsoft Acrylic](https://learn.microsoft.com/en-us/windows/apps/design/style/acrylic) 对模糊、混合层和底色叠加的说明。本项目采用背景模糊与页面底色叠加，具体数值由本项目 Token 管理。

实现由 [共享设计样式](frontend/src/styles/design-language.css) 的 `::before` 提供：`backdrop-filter: blur(...)` 对背景执行高斯模糊，`mask-image: linear-gradient(...)` 让模糊效果逐渐透明。模糊半径使用 `--floating-backdrop-blur`（16px），渐隐距离使用 `--floating-backdrop-fade`（48px），数值统一维护在 `tokens.css`。聊天与医疗报告列表的悬浮操作区、会话输入区直接复用此实现。添加悬浮容器声明 `data-floating-edge="bottom"` 或 `data-floating-edge="top"`，并使用定位和非 `auto` 的 `z-index` 建立自身层叠上下文；容器允许材质层延伸到栏位左右边界、靠边留白及朝向正文的渐隐区域；限宽控件必须独立计算所属栏位的完整材质宽度。顶部属性自动反转遮罩方向。浏览器不支持背景模糊时保留控件原有可读表面。

### 分组列表

`GroupedList` 的每次使用都必须显式填写 `density="standard"`（标准格式）或 `density="compact"`（紧凑格式），不提供默认值。格式与字段、导航、选择或操作布局独立组合。同一列表内统一使用所选格式，嵌套列表按自身格式重新确定尺寸。

| 格式 | 单行最小高度 | 字号 / 行高 | 垂直 / 水平内边距 | 图标 | 内嵌图标按钮 | 列表与交互条目圆角 |
| --- | --- | --- | --- | --- | --- | --- |
| 标准 | 40px | 15px / 20px | 10px / 10px | 16px | 20px | 10px |
| 紧凑 | 30px | 13px / 18px | 6px / 10px | 14px | 18px | 7.5px |

标准格式用于常规浏览与编辑，紧凑格式用于密集信息。共享列表样式统一控制条目、字段输入、图标和内嵌操作的尺寸；页面只选择格式与布局。复选框和开关保留各自专用尺寸，多行条目随内容自然增高。

`GroupedList` 提供一个无外框、无投影的控件表面，两种格式的相邻条目都共享内部分隔线。分隔线两端各内缩控件间距。静态条目透明，交互时只突出对应条目并隐藏相邻分隔线。首项、中间项和末项的交互表面均为完整四角圆角，不依赖容器裁切；整行内部按钮继承条目轮廓，页面不得另设圆角或交互表面。展开容器的标题与内容拼接边仍遵循容器规则。

列表内容可为单个或多个条目；字段、导航和只读内容不另设表面。富内容条目自然增高，上下保留控件间距。普通列表内的添加入口统一放在首项，包括模型、检验分类与指标目录、成员选择、药品批次、关联医疗报告、用药时间、食物和睡眠阶段。带列标题的数据表将添加入口放在表格下方，顶部保留列标题与数据的连续阅读顺序，例如医疗报告内的检验指标。工作区级创建按钮（发起新聊天、创建医疗报告、创建健康日记、添加药品、创建用药计划）固定悬浮在所属列表栏底部，列表末端预留其高度与内容间距。

列表末尾的数量说明统一为“共 N＋量词＋完整对象名称”，例如“共 7 份医疗报告”“共 12 条健康日记”“共 6 条检验指标”；对象名称与内容栏标题一致，目录使用对应对象名称。数量表示当前筛选条件下的总数，分页列表也使用此格式，通过“加载更多”继续读取。多选计数同样使用完整对象名称。数量说明紧随上方列表，水平居中，与最后一条列表内容的下缘保持 15px 实际间距，使用 `--space-content`。数量说明与列表处于同一滚动内容流，按内容自然排列；短列表时也贴随列表，不被弹性剩余空间推到栏位底部，不参与空状态的垂直居中。间距由一处承担，不叠加列表底部内边距、容器间距和计数顶部外边距。计数不包含创建入口，零数量不单独显示。

根级折叠列表是可折叠的列表容器，各组独立，组间使用内容间距。标题使用标题表面，展开时显示下缘分隔线。展开状态、可访问名称和方向箭头保持一致，内容复用列表条目。数量、滚动上限和默认展开状态由页面契约决定。

### 数据表、正文与面板

跨列比较的同构记录使用数据表，不强制改成字段条目。连续阅读使用正文；面板用于非列表独立内容，不在列表外再包面板。媒体使用透明预览舞台，不附加装饰性表面。

### 覆盖层

可删除的对象列表及详情内的关联、明细列表统一通过上下文操作覆盖层提供“多选”“删除”，包括关联医疗报告、药品批次与固定用药时间。支持右键、触屏长按和键盘菜单键，条目不常驻删除按钮；多选时显示左侧复选框、选择数量、全选、退出与批量删除操作。Escape 优先退出当前多选，保留所在页面。关联条目的删除只解除对应关联。

覆盖层挂载到页面顶层，不受工作区裁切。选择菜单的宽度与水平对齐分别由使用场景明确指定，单选和多选共用以下规则：

- 独立选择框、医疗报告类型筛选与检验指标分类筛选：菜单与实际触发控件等宽，左右边缘对齐，每个选项的可点击区域覆盖菜单整行。
- 字段行右侧的值选择：菜单宽度由最长标签及必要的图标、复选框与内边距决定，右边缘与实际选择控件对齐，向左延展。整行高亮只表达操作状态，不决定菜单宽度或定位锚点。
- 紧凑菜单：按内容定宽，左侧入口左边缘对齐，右侧入口右边缘对齐。紧凑成员选择与授权权限选择使用右边缘对齐。

选择菜单锚定实际触发控件，默认向下展开，下方空间不足时向上展开。菜单最大宽度与高度受可视区域安全距离约束，水平位置在所选对齐基础上避让边缘，过长内容在菜单内部滚动。滚动、窗口缩放、触发控件或菜单内容尺寸变化后重新定位；可用空间恢复时，菜单恢复所选规则下的宽度。

共享单选、多选组件通过必填的 `menuWidth="trigger|content"` 与 `menuAlign="start|end"` 分别指定宽度与对齐，不根据类名、单选或多选类型、交互高亮归属推断。上下文菜单按内容定宽，根据触发位置定位，并遵守相同的可视区域安全距离。

全站系统生成的日期显示统一使用 `YYYY年M月D日`（如“2026年9月7日”），月日不补零；需要时间时追加 24 小时制 `HH:mm`（如“2026年9月7日 14:05”）。医疗报告与健康日记的日期分组、详情字段、日期范围、出生日期、收藏时间、用药计划周期、药品批次日期、用药提醒及日期选择器的无障碍名称共用 `frontend/src/utils/localTime.ts` 的格式函数。纯日期按原日历日期显示，所有带时区时间均按浏览器本地时区显示，包括收藏医疗报告快照与用药提醒，不显示原时区标识或提供时区选择。月份精度统一显示为 `YYYY年M月`，不补造日期；日期范围用“至”连接。具体日期的接口值、表单值与 `time` 元素的 `dateTime` 属性使用 ISO 格式；长期和未知按字段契约表示；用户正文和来源原文按内容呈现。

日期与时间选择器共用 `DateTimePicker` 和 `date-time-picker.css`。年月日输入、时间输入、日历格、切月按钮及底部按钮统一使用紧凑控件：13px / 18px 文字、30px 最小高度，图标为 14px；年月标题同样使用 13px / 18px，加粗显示。宽度使用 `--size-date-picker-width`（300px），受视口安全距离约束；触屏继续遵守至少 44px 的实际点击范围。年月日输入按列对齐，不显示重复字段标签，通过无障碍名称标识用途；今天用短下划线标记，已选择日期沿用单选状态。

日期选择器底部按字段能力提供快捷按钮，业务适用范围与保存含义见[日期选择与空值](docs/当前执行/前端/行为与交互.md#日期选择与空值)。快捷操作位于左侧，取消与完成位于右侧，空间不足时操作组自然换行，均复用紧凑控件。覆盖层使用共享边框、圆角、阴影和安全距离，内容超出高度时在层内滚动。

所有“添加”“创建”入口点击后直接打开居中内容弹窗，包括列表对象、关联选择及表单内的子条目。添加表单在弹窗内填写，点击“完成”后将有效内容加入列表或保存；关闭及 Escape 不产生空条目。已在弹窗中的添加入口打开子弹窗，并沿用统一模态焦点规则。

创建窗口统一使用 `creation-dialog` 外壳，包括创建医疗报告、药品、计划、健康日记、身体指标记录、成员、检验分类与指标，以及添加模型、批次、标签、关联医疗报告、用药时间、食物和睡眠阶段。桌面标准宽高由 `--size-creation-dialog-width` 与 `--size-creation-dialog-height` 管理；实际可用宽高不足时，分别收缩到视口两侧、上下各保留 15px 的范围。同一视口下所有创建窗口和其各级步骤大小一致，短表单也保持标准高度，长表单在正文区域滚动。

创建窗口标题居中，右侧为关闭图标按钮；存在上一级步骤或下层表单时，标题栏左侧显示“返回上一级”图标按钮，与右侧关闭按钮使用相同尺寸和边缘间距。返回只回到上一层，子窗口返回保留下层草稿；底部不再提供“取消”或文字“返回”按钮。关闭与 Escape 沿用当前窗口的关闭语义和提交期间保护。

创建窗口的最终提交操作统一标为“完成”，使用勾形图标，不再按对象使用“保存医疗报告”“保存成员”“添加”等提交文案。选择方式、类型或文件等中间动作保留其具体名称。“完成”位于窗口最底部的普通操作区，桌面靠右、手机按共享操作区规则占满可用宽度；操作区与正文是相邻布局区域，不使用绝对定位、悬浮、粘性定位或覆盖正文。正文为操作区让出空间，长内容滚动时按钮保持可达；间距继续遵守下方内容弹窗留白规则。提交失败保留草稿及“完成”按钮，允许修正或重试；正在提交时可以显示进度文案。多选后立即生效的添加模型窗口以“完成”结束选择并关闭窗口。

内容弹窗相对可视区域居中，复用标题栏、正文与可选底部操作区；正文和操作区使用内容间距。内容弹窗在窄屏继续居中，两侧和上下各保留至少一个 15px 内容间距；宽度受遮罩可用宽度约束，高度使用动态可视区域限制，正文在弹窗内滚动，标题栏和底部操作区保持可达。长标题单行省略，关闭控件保留完整尺寸；窄屏底部操作按钮纵向排列。媒体预览在窄屏可占满动态可视区域，各屏幕宽度均使用居中标题和等宽左右操作列；操作列按实际按钮宽度保留空间，长标题单行省略。只有覆盖层使用 `--shadow-overlay`，页面流内不使用投影。

弹窗的相邻区域按最终可见距离计算留白，不叠加各自的内边距。标题栏下缘到正文为 15px；正文末项到操作按钮为 15px，操作按钮到弹窗底部内缘也为 15px，边框占位另计。正文和底部操作区作为相邻区域排列，不把操作区嵌入已带内边距的正文。正文保留底部内容间距，紧接其后的操作区不再增加顶部内边距；没有底部操作区时，正文独自保留底部 15px。正文发生滚动时，在滚动末端验证末项到按钮或底边的间距。水平留白同样只保留一层内容间距，正文与操作区保持同列对齐。

选区上下文操作浮层是覆盖层中的单一动作控件，不是提示气泡、菜单或新的按钮外观。用户在可注释正文中选取有效文本时，它固定定位于选区上方，并在视口左右边缘留出安全距离。控件本体使用 `--color-control-surface`、`--color-foreground` 和 `--color-divider`，采用标准控件高度、圆角、图标尺寸、内边距与 `--layer-menu`；鼠标悬停、键盘焦点和禁用状态完全复用标准控件交互规则。浮层随选区失效、添加成功、外部操作或 Escape 消失，不保留反色专用表面、边框或交互状态。

## 4. 条目与控件

### 条目布局

- 字段：列表共享标签列与值列，标签列取组内最长标签宽度，值列占满剩余空间。标签左对齐，紧凑值右对齐；可编辑单行内容内部横向滚动，只读紧凑值省略。
- 长文本字段：设置类说明使用元信息规格，单行右对齐、多行左对齐并自动增高；连续阅读的结构化医疗报告事实使用正文规格、左对齐与自然换行。
- 导航：横向排列时，标签、当前摘要、右箭头共享列轨。摘要使用元信息规格，单行右对齐、多行左对齐，箭头不被长文本挤出。主文字与摘要也可以在同一条目内上下排列，遵循下方组合规则。
- 选择：复选框位于文字左侧；单选条目只通过选中背景表达当前值，不显示对勾、复选框、单选圆点或开关。
- 操作：一项只承担一个主要意图；列表内操作继承条目尺寸与状态，不嵌入另一层按钮表面。创建入口的图标与文字紧邻、整体居中，不复用导航条目的分列布局。

同一标准条目内的主文字与摘要可以上下组合：上方主文字使用 15px / 20px、常规 500 和主要前景，下方摘要使用 13px / 18px、常规 500 和次要前景，两者左对齐，垂直间距使用 `--space-related`（5px）。这是一个条目内的两种文字角色，共用整条的内边距、圆角、分隔线及交互状态；条目按内容自然增高，40px 是最小高度。存在进入箭头时，箭头位于文字组右侧并垂直居中，不被长文本挤出。主文字默认单行省略，摘要行数由功能契约规定；限制单行时超出宽度显示省略号，完整内容在详情中查看。健康日记列表采用这一组合，摘要只显示一行。

只读值使用静态信息项，编辑使用真实输入控件并正确关联标签。局部校验操作位于相关列表末项，表单提交位于列表外。敏感文本统一掩码并使用可见性按钮。

### 控件组合

输入框统一不绘制常驻边框，也不使用内阴影模拟描边；独立输入、字段内输入、会话输入和可点击编辑的正文展示区域均遵守此规则。输入区域沿用所属控件的圆角、内边距和表面，通过悬停与实际输入焦点表达状态；键盘导航继续使用统一焦点轮廓。

按钮分别选择尺寸、外观和语义：主要操作使用当前风格的主要表面与主题文字色，次要操作使用控件表面，标准与紧凑内嵌图标操作默认透明；危险语义改变配色，不改变尺寸。选区上下文操作浮层复用次要操作外观，不添加反色按钮变体。同一操作组使用相同文字与尺寸。独立危险按钮默认使用危险表面，悬停改用危险操作悬停表面；菜单内危险操作默认使用主题文字色与危险图标，悬停时使用同一危险操作悬停表面。危险按钮在默认、悬停与禁用状态均不绘制边框，键盘导航时仍显示全局焦点轮廓。

输入、单选、多选和密钥控件共享控件尺寸与焦点规则；位于字段条目内时使用透明表面，不重复绘制内层边框。交互归属不按 HTML 标签判断，而按意图判断：主要意图由条目承接，独立意图由控件自身承接。原生可编辑输入由字段或输入表面直接承接；嵌套触发器使用 `data-interaction-owner="row|self"` 明确归属。字段选择、整行选择和主要导航使用 `row`，筛选器、工具栏及同一行中的独立操作使用 `self`。独立下拉控件由触发器承接。单选完成后关闭菜单，多选保持展开。

控件按用户操作的含义选择；是否自动保存不作为选择开关或勾选的依据。

| 场景 | 使用控件 | 位置与示例 |
| --- | --- | --- |
| 启用或停用一项独立功能 | 开关 | 标签在左、开关在右，例如站内通知、联网功能；开与关分别表示功能启用和停用 |
| 从一组选项中选择多个成员 | 左侧复选勾选 | 勾选框在文字左侧，整行可点，支持独立选择和取消；例如用药星期、关联医疗报告和批量选择记录 |
| 从互斥选项中确定一个值 | 单选列表 | 只通过选中背景表达唯一当前值，例如用药频率；不显示对勾、复选框、单选圆点或开关 |
| 展示成功、失败、完成或连接结果 | 状态图标或文字 | 不可点击改变结果，不使用开关或可交互复选框 |

用药星期表示“每周选择哪些天”，即使选择后立即自动保存，也使用左侧复选勾选。多选列表声明 `selectionMode="multiple"`，条目复用 `GroupedCheckboxRow` 与共享勾选样式，使用 `role="checkbox"` 和 `aria-checked`；整行响应点击及空格键，只读时保留勾选结果并禁用操作。勾选只改变左侧标记，不给已勾选行持续加深背景；悬停、圆角、分隔线和键盘焦点遵循共享列表规则。开关复用 `Switch`，使用 `role="switch"` 和 `aria-checked`。

未勾选复选框的边框、关闭状态开关的边框与滑块使用次要前景色；开启状态使用强调色轨道与主表面色滑块，使控件轮廓和位置始终清晰可辨。成功、失败、完成、复制成功和连接状态使用状态图标或文字，不借用复选框、开关表达结果。

### 分档滑块

分档滑块使用原生范围输入，只允许选择当前对象支持的离散档位；刻度按档位等距排列，当前值以文字显示在左侧。模型选择与分档滑块组合时，上方直接显示可点击的模型名称，两行复用标准 `GroupedList`，模型名称与当前等级均为 15px / 20px、常规 500，不重复显示字段标签；无障碍名称仍保留用途。

滑块轨道高 16px、圆角 4px，拖动块为 20 × 20px、圆角 5px，刻度为 4 × 4px、圆角 1px，分别由 `--size-slider-*` 与 `--radius-slider-*` Token 管理。全部采用短边四分之一的圆角矩形，不使用圆形拖动块、胶囊轨道或投影。轨道以分隔线色承载未选区、强调色承载已选区，拖动块使用主表面与 1px 次要前景边框；填充仅表达数值进度，不使用装饰性渐变。原生范围输入的轨道保持透明，由共享样式统一绘制完整四边轮廓，避免浏览器默认轨道遮住边框。

共享 `.discrete-slider` 管理控件外观，标准字段条目承接悬停、实际输入焦点与键盘轮廓：悬停使用悬停表面、聚焦编辑使用当前状态表面、四角完整圆角，高亮时隐藏相邻缩进分隔线，退出后恢复。粗指针环境扩大实际操作目标到至少 44px，轨道、刻度与拖动块的视觉尺寸不变。支持拖动、点击轨道、方向键逐档调整及 Home / End 选择两端；用 `aria-valuetext` 提供档位名称。调整时保持弹层打开；仅有一个可用档位时禁用滑块并继续显示当前值。

## 5. 状态与交互

### 状态职责

| 状态 | 表达与边界 |
| --- | --- |
| 默认 | 条目继承容器表面，控件使用所属外观 |
| 鼠标悬停 | 可用、未选中、未编辑的控件与只读条目使用悬停表面 `--color-item-hover`；条目正文、空白区域及 `row` 主要控件由整个条目承接悬停；指针进入 `self` 附属操作时条目撤销临时悬停，仅该操作显示悬停表面；控件本身就是条目时由该控件承接完整条目表面；移开且未打开上下文菜单时恢复，不产生光圈 |
| 正在编辑 | 字段整行、独立表单输入框自身、会话输入表面及用户消息编辑表面使用当前状态表面 `--color-item-current`，实际可编辑输入失焦或结束编辑后撤销；次级表面只规定默认底色，不取消悬停或编辑状态 |
| 当前页面、单选、已开启的状态按钮 | 持续使用当前状态表面；悬停不能覆盖 |
| 下拉菜单展开 | 字段中的选择控件由整行使用当前状态表面，独立选择控件由触发器使用该表面；关闭恢复，已有值不触发高亮 |
| 上下文菜单打开 | 触发条目整行保持悬停表面 `--color-item-hover`，鼠标移入菜单或离开条目后仍保留完整圆角与相邻分隔线隐藏；已有当前对象、单选或编辑表面优先。菜单关闭后恢复条目的实际状态 |
| 键盘导航焦点 | 在已有状态色上叠加清晰的 2px 强调色轮廓，不改变底色；鼠标点击后取消轮廓 |
| 多选 | 仅复选框表达持久勾选状态；条目不因勾选改变表面或分隔线，悬停使用悬停表面，键盘导航只叠加轮廓。进入批量选择时不沿用浏览中的当前对象高亮 |
| 禁用、运行 | 不响应悬停；保留操作位置、原有状态含义与语义色相，弱化显示并禁止重复提交 |
| 成功、失败 | 优先通过值、列表、按钮文字或图标反馈；需解释时使用右上角通知 |

当前对象、单选、编辑与展开不是同一业务状态。编辑允许复用当前状态表面，但不能给编辑字段伪加 `aria-current`；多选不能借用当前对象表面。普通按钮不会因为点击过而持续高亮，折叠标题不会因为展开而成为当前对象。瞬时结果不保存为当前状态；可重复操作只在请求中禁用，结束后恢复。

判断示例：字段下拉与行内日期选择用于更新该字段的唯一值，归属 `row`；侧栏聊天、医疗报告条目和模型提供方的主要选择动作代表整行意图，也归属 `row`。筛选下拉只改变工具栏条件，归属 `self`。执行记录的披露标题是独立的展开控件，归属 `self`，只改变标题表面，不把正文容器视为可点击条目。授权创建行以档案勾选作为主要意图，勾选控件归属 `row`，同行的权限选择归属 `self`；勾选后的持续状态仍只由复选框表达。已授权行没有整行主操作，静态档案与接收方身份不产生悬停，权限选择和撤销分别归属 `self`。等待队列、收藏标签等复合条目中的拖动、编辑、删除、显隐与移除也各自归属 `self`；不得因控件都是按钮或都位于同一行而套用同一种悬停表面。

上下文菜单的触发条目高亮随菜单存续，右击、长按和键盘打开遵守同一规则，不改变当前对象或多选勾选。切换到另一条目的菜单时，高亮随触发条目转移；执行操作、取消、外部点击、来源祖先滚动或导航关闭菜单后撤销临时高亮，鼠标仍悬停时保留正常悬停颜色。

### 编辑归属与优先级

- 字段条目是唯一的整行底色与字段主要控件焦点轮廓拥有者，内部输入和 `row` 选择触发器透明。独立表单输入框拥有自身底色。文本、密码、数值、日期时间、多行、搜索与医疗报告正文编辑遵守同一规则；会话重命名属于侧栏条目内编辑，由整行承接，输入框不得另画边框。
- 会话输入框的外容器连同工具栏，以及用户消息气泡，默认使用次级表面 `--color-rail`。指针位于文字输入区域时外容器使用悬停表面，实际可编辑输入聚焦时使用当前状态表面；内部文本输入始终透明。附件、模型、用量、发送和消息编辑操作归属 `self`，分别显示自身反馈；它们获得焦点后外容器恢复其实际状态。若文字输入仍保持焦点，仅把指针移到附属操作不会覆盖持续编辑状态。附件与引用链接显示悬停或当前状态表面时，同步采用正文颜色保证可读性。
- 编辑只取实际可编辑输入焦点，不取已有值、未保存草稿、保存请求状态或页面编辑模式属性。焦点从“用户标识”转到“账号名称”时深色跟随移动；失焦后即使保存失败或仍展示编辑器，也不能保留编辑底色。若鼠标仍在原字段上，恢复为浅色悬停。
- 只读内容、复选框、文件输入和下拉触发器不算文字编辑。内嵌显隐、清除等按钮拥有自己的悬停、状态色和轮廓，不能借其键盘焦点让字段整行显示编辑表面。
- 悬停规则使用低优先级；编辑、当前对象、单选、已开启状态和展开触发器的深色优先，键盘轮廓独立叠加。多选容器明确声明 `selectionMode="multiple"` 或 `aria-multiselectable="true"`，通用按下状态不能染深复选条目。
- 主操作、危险按钮、复选框及开关本体保留现有语义配色，只统一交互优先级。列表分隔线不能穿过高亮或焦点轮廓；取消高亮后恢复原分隔线。

### 键盘与可访问性

图标按钮提供 `aria-label`，装饰图标使用 `aria-hidden="true"`；状态不能只依赖颜色。所有交互必须支持键盘，焦点移动不引起无关滚动。

应用统一跟踪输入方式。鼠标点击取消轮廓，Tab、方向导航、键盘确认或取消引起的焦点转移显示轮廓；普通打字、输入法组合和鼠标移动不切换输入方式。不能直接以原生 `:focus-visible` 判断输入方式，因为鼠标点入文本框也可能匹配它。菜单、弹窗的初始聚焦与焦点归还沿用打开或关闭动作的输入方式。

同一焦点只绘制一层轮廓：分组条目内描边，独立控件外描边。`row` 控件聚焦时标记最近的字段条目、行表面或分组条目；`self` 控件聚焦时只标记自身。字段输入或选择触发器聚焦时标记整行；显隐、移除等内嵌操作按钮聚焦时只标记按钮，不同时标记行和输入框。键盘轮廓不替代当前状态色，也不额外增加悬停底色。

所有 `aria-modal="true"` 的正式模态使用统一焦点契约：打开时背景工作区不可交互；子弹窗打开时，下层弹窗同时不可交互且不接收焦点或 Escape，子弹窗关闭后恢复下层；初始焦点落在标题，需立即输入时可落在主要输入。Tab 与 Shift+Tab 在模态及其顶层选择菜单内循环，Escape 优先关闭内部菜单，操作未锁定时关闭或返回模态。关闭后归还来源焦点；来源已移除时不强行聚焦失效节点。

### 响应式与反馈文案

手机布局、平板布局和宽屏布局共用一套组件、信息层级和布局规则，随可用宽度调整展示方式。标题栏、内容分组、列表、字段、控件与覆盖层沿用同一设计语言，同一功能在三种布局中保持相同的信息关系和操作含义。三档名称、准确宽度边界及各档栏位行为集中定义在[前端行为文档的三档布局与栏位行为](docs/当前执行/前端/行为与交互.md#三档布局与栏位行为)。

断点只改变栏位、排列、显隐、换行、滚动和覆盖层位置，不改变同一组件的字号、行高、尺寸、圆角、字重及状态含义。弹窗、菜单、长文本与数据表不得超出可视区域。启用减少动态效果偏好时停止非必要动画，保留静态状态线索。

### 空状态

空列表、筛选无结果和未选择详情使用共享 [EmptyState](frontend/src/components/EmptyState.tsx)，外观只由 [components.css](frontend/src/styles/components.css) 定义。页面负责分配其所在区域，不自行设置提示的字号、字重、颜色或偏移。

| 场景 | 布局与文字 |
| --- | --- |
| 独立列表为空、搜索或筛选无结果、尚未选择详情 | 使用 `pane`，在所属栏位的剩余可见内容区上下左右居中；主提示使用正文规格、加粗和主要前景色 |
| 已有内容中的局部区域为空，例如待处理通知、导入记录、图表、关联条目与弹窗内药品列表 | 使用 `inline`，在所属内容区域水平居中，保持自然高度；提示使用说明文字规格、常规字重和次要前景色 |
| 需要解释下一步的空状态 | 在主提示下使用说明文字规格和次要前景色，间距为关联间距；说明只提供补充的信息 |
| 缺失字段值、摘要卡片中的“暂无记录”、原件缩略图、可编辑正文的空值 | 保持所属字段、卡片、缩略图或编辑控件的布局，使用对应组件的规则 |

“剩余内容区”从标题、成员身份栏、功能入口、搜索筛选之后开始，到底部固定或悬浮操作、输入区的预留区域之前结束。以每个栏位自己的可用区域为准。左右居中同时计入滚动条实际占位与两侧留白；切换筛选、调整窗口和侧栏开合后自动重算。手机只对当前可见层级执行同一规则。

父容器必须沿布局链提供确定的剩余高度，并允许内容区收缩与滚动。空列表使用可伸展的网格行或弹性剩余区域，详情滚动区在空状态下也须撑满；单独增加 `text-align: center`、对没有确定高度的父级使用 `height: 100%` 或使用固定上边距都不能满足此规则。空提示不与空列表节点、零数量或空文本节点争用网格行。只有实际存在底部悬浮操作时才为它预留高度。

提示通常是一句短文，窄屏允许自然换行，整体仍居中，不截断。普通空状态不增加装饰性图标或卡片；已有带图标的详情引导通过共享组件传入图标，图标与文案作为整体居中。可创建列表保留既有创建入口，搜索和筛选继续可用；不在提示中重复添加同一按钮。零数量不单独显示，非零计数的量词与对象一致。加载中与读取失败显示对应状态，成功读取后才能显示无数据提示。

浏览器验收需要测量文案组相对可用内容区的水平与垂直中心，不能只检查 CSS 类名或 `text-align`。覆盖宽屏、平板、手机、浅色与深色，以及筛选无结果、有数据后清空、底部操作存在与不存在；同时确认提示未被按钮遮挡，局部空状态未撑高整页。

## 6. 实现与维护

- Token 保存数值和语义角色；元素重置、基础继承、滚动条及全局可访问性归入基础样式。
- 扩展标准控件的内容和身份信息条归入 `design-language.css`，日期选择器归入 `date-time-picker.css`，原件缩略图、预览及其响应式规则归入 `file-preview.css`；共享组件使用通用名称，不依赖某个业务页面的样式文件。
- 共享设计样式负责容器、条目、控件与状态；内容样式负责 Markdown 等内容渲染；页面样式只负责布局、内容适配和明确的业务表现。
- `interaction-states.css` 集中管理状态组合，`interactionFocus.ts` 管输入方式与唯一轮廓归属。页面不得覆盖悬停、编辑和焦点配色；移除被替代的规则，不叠加高优先级补丁。
- 新规则先确定所属层级和现有角色。两个页面需要同一修正时提升到共享层并删除局部重复，不增加同义类名、兼容别名或提高选择器权重的补丁。
- 调整共享规则时同步所有调用点和相关测试，以实际布局、状态及交互验收，不要求保留已替代的实现结构。
- 浏览器验收优先操作真实页面和真实组件。只有滚动、动态尺寸等必须依赖真实 DOM 几何的行为才使用最小测试页面；只有实现存在断点或浏览器分支时才扩展视口、Chromium 或 WebKit 矩阵。测试验证用户可观察结果，不以源码字符串、CSS 选择器文本或重复像素常量代替行为，也不更新真实业务数据。
- 前端行为文档只引用此处角色；技术契约维护接口与持久化，README 只提供入口与开发说明。未来愿景不参与当前执行。
