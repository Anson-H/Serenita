# Serenita 设计语言

> 当前执行 / 全局视觉契约。页面结构与业务交互见 [前端行为文档](docs/当前执行/前端/行为与交互.md)。

## 1. 职责与层级

本文件定义跨页面复用的视觉角色及其组合规则。产品定位见 [PRODUCT.md](PRODUCT.md)，业务字段顺序、入口名称、保存策略和接口见对应功能契约。实现数值以 [tokens.css](frontend/src/styles/tokens.css) 为准，共享外观由 [design-language.css](frontend/src/styles/design-language.css) 实现，状态组合由 [interaction-states.css](frontend/src/styles/interaction-states.css) 管理。

### 信息层级

| 层级 | 职责 | 例子 |
| --- | --- | --- |
| 工作区 | 承载当前任务或对象，包含标题栏与内容区 | 报告详情 |
| 内容分组 | 分组标题与其管理的相关内容 | “基础信息”及其下方内容 |
| 内容容器 | 按内容关系选择分组列表、数据表、正文或面板 | 基础信息使用分组列表 |
| 条目 | 分组列表中的一个内容单位，可采用字段、导航、选择或操作布局 | “就诊时间”标签与其值 |
| 控件 | 条目内或独立使用的输入、选择、按钮、复选框、开关 | 就诊时间输入控件 |

这是信息组织关系，不要求每一层都增加 DOM 包装。分组标题位于内容容器外；标签、值和控件属于条目内部。术语按此层级使用，不把条目的布局再命名为另一种容器。

覆盖层是显示在页面流之上的层级，不是与分组列表并列的容器。选择菜单、上下文菜单、内容弹窗和媒体预览位于覆盖层，内部继续复用相同的容器、条目和控件规则。

### 设计取向

患者和家属可能在焦虑状态下长时间阅读与核对报告，界面采用活泼、温暖的杏橙与暖瓷白浅色体系。暖瓷白与浅杏色承载长时间阅读，明亮杏橙集中用于主要操作和品牌标记，主要前景统一使用纯黑色，覆盖文字、连续正文与普通图标，保持清晰对比。优先用栏位、对齐、留白和分隔线组织信息，不使用嵌套卡片、装饰性渐变或无必要动画。持续浏览和编辑在原位置推进，技术细节按需展开。

## 2. 视觉基础

### 颜色

颜色按职责引用 Token，基色使用 OKLCH，派生色通过基础 Token 混合生成。下表按 `tokens.css` 的定义顺序列出全部颜色 Token，以及包含颜色的焦点与阴影 Token，说明各自的名称、取值与用途。颜色名用于直观描述外观，实际数值及混合比例统一在 `tokens.css` 维护。文字颜色属于颜色规范；字体、字号、行高和字重由独立的文字规范定义。

浏览器画布保留默认背景，`:root` 与 `body` 不设置背景色；登录页、工作区外壳与错误页的应用背景统一使用主表面。控件、导航侧栏、悬停与选中表面沿杏色系逐级加深并提高饱和度，同时区分默认底色与交互状态。

#### 表面、前景与分隔

| 角色 | 颜色名 | Token | 色值 | 用途 |
| --- | --- | --- | --- | --- |
| 画布 | 浏览器默认 | 不定义 | 由浏览器决定 | 页面最底层背景 |
| 主表面 | 暖瓷白 | `--color-surface` | `oklch(99% 0.004 75)` | 主内容、菜单与内容弹窗 |
| 次级表面 | 杏米色 | `--color-rail` | `oklch(94% 0.026 70)` | 导航侧栏、会话输入区域与账号头像 |
| 标题表面 | 深杏米色 | `--color-header-surface` | `oklch(92% 0.034 70)` | 折叠标题与表格表头 |
| 控件表面 | 淡杏白 | `--color-control-surface` | `oklch(97.022% 0.01432 70.7)` | 分组列表、标准/紧凑控件（除会话输入区域外）|
| 高亮表面 | 明亮杏橙 | `--color-primary-surface` | `oklch(76% 0.16 58)` | 高亮按钮与品牌标记 |
| 内容衬底 | 淡杏白 | `--color-content-inset` | `oklch(97.022% 0.01432 70.7)` | 附件、Markdown 行内代码、引用块与资源引用标签，以及引用预览与上下文用量提示的底色和边框 |
| 主要前景 | 纯黑 | `--color-foreground` | `oklch(0% 0 0)` | 标题、标签，控件、条目与条栏文字，连续正文，以及导航、操作与通用功能图标 |
| 次要前景 | 暖褐灰 | `--color-foreground-muted` | `oklch(48.5% 0.022 65)` | 元信息、辅助说明与次要摘要，以及弱化的控件轮廓与轨道 |
| 分隔线 | 浅暖灰 | `--color-divider` | `oklch(84% 0.012 70)` | 栏位边界、标题栏下缘与列表内部分隔，以及附件、输入和操作控件边框、上下文用量圆环轨道 |
| 警告表面 | 淡奶黄 | `--color-warning-soft` | `oklch(95.5% 0.045 88)` | 警告内容的辅助表面 |
| 警告前景 | 赭黄 | `--color-warning` | `oklch(49% 0.095 82)` | 需要注意、核对或处理的提示 |
| 危险表面 | 浅玫瑰白 | `--color-danger-soft` | `oklch(94% 0.024 25)` | 独立危险控件与危险提示的默认辅助表面 |
| 危险前景 | 深朱红 | `--color-danger` | `oklch(51% 0.18 25)` | 删除等危险操作、错误与失败反馈的文字和图标 |

#### 强调、交互与状态

| 角色 | 颜色名 | Token | 色值 | 用途 |
| --- | --- | --- | --- | --- |
| 通用强调 | 深橙 | `--color-accent` | `oklch(57% 0.15 50)` | 复选框勾选、开关开启、键盘焦点轮廓及强调标记 |
| 表面悬停 | 浅杏色 | `--color-item-hover` | `oklch(91% 0.047 65)` | 可用、未选中、未编辑条目与控件的鼠标悬停 |
| 选中悬停 | 暖杏色 | `--color-item-current` | `oklch(89% 0.07 60)` | 当前对象、单选与已开启状态按钮；编辑和展开触发器复用此深色 |
| 高亮表面悬停 | 浓杏橙 | `--color-primary-hover` | `oklch(71% 0.17 53)` | 主要按钮的悬停表面 |
| 危险表面悬停 | 玫瑰红 | `--color-danger-hover` | `oklch(90% 0.045 25)` | 独立危险控件与危险菜单项在鼠标悬停时的表面 |
| 成功 | 深翠绿 | `--color-success` | `oklch(47% 0.105 155)` | 操作成功、连接成功等已确认结果 |

#### 补充表面、前景与分隔

| 角色 | 颜色名 | Token | 色值 | 用途 |
| --- | --- | --- | --- | --- |
| 错误提示混合基色 | 暖米白 | `--color-surface-error` | `oklch(97.7% 0.012 70)` | 会话错误提示的底色混合 |
| 模式切换与记录表面 | 白色 | `--color-surface-raised` | `oklch(100% 0 0)` | 登录页模式切换按钮与执行记录的底色 |

#### 反色表面、前景与边框

| 角色    | 颜色名   | Token                     | 色值                           | 用途                         |
| ----- | ----- | ------------------------- | ---------------------------- | -------------------------- |
| 反色表面  | 深暖灰   | `--color-inverse-surface` | `oklch(24% 0.018 70)`        | 深色代码区底色，以及覆盖层遮罩的基色 |
| 反色文字  | 暖灰白   | `--color-inverse-text`    | `oklch(96.5% 0.009 70)`      | 深色代码区文字        |
| 反色分隔线 | 暖墨灰   | `--color-inverse-divider` | `oklch(32% 0.018 70)`        | Markdown 深色代码块边框           |

#### 滚动条、引用与进度

| 角色 | 颜色名 | Token | 色值 | 用途 |
| --- | --- | --- | --- | --- |
| 滚动条滑块 | 半透明暖灰 | `--color-scrollbar-thumb` | `oklch(70% 0.018 70 / 0.42)` | 全局滚动条滑块的默认颜色 |
| 滚动条滑块悬停 | 半透明深暖灰 | `--color-scrollbar-thumb-hover` | `oklch(60% 0.018 70 / 0.55)` | 滚动条滑块的悬停颜色 |
| 上传进度 | 浅翠绿 | `--color-upload-progress` | `oklch(82% 0.105 155)` | 附件上传进度条的已完成部分 |

#### 焦点与阴影

| 角色 | 颜色名 | Token | 色值 | 用途 |
| --- | --- | --- | --- | --- |
| 键盘焦点轮廓 | 深橙 | `--focus-outline` | `oklch(57% 0.15 50)` | 键盘焦点使用的 2px 实线轮廓 |
| 覆盖层阴影 | 半透明深暖灰 | `--shadow-overlay` | `oklch(31% 0.018 70 / 0.13)` | 覆盖层投影；水平偏移 0、垂直偏移 18px、模糊半径 44px |

底色与焦点轮廓分开管理。悬停不能覆盖深色状态；多选的持续状态只由复选框表达。键盘导航焦点使用 `--focus-outline`，不使用模糊光圈，也不改变已有底色。禁用与运行状态保留所属色相，通过降低对比度表达不可操作，不另设状态色。状态的具体交互边界见第 5 节。

颜色只辅助传达信息，状态同时提供可辨认的文字或图标。普通文字对其实际背景的对比度至少为 4.5:1，必要控件标识与焦点轮廓至少为 3:1。禁用状态沿用既有弱化规则。不能仅以文字变浅来表达错误或重要提示。

### 文字

界面使用 `--font-ui`，代码使用 `--font-mono`。连续正文限制在 `65–75ch`，结构化字段与数据使用容器可用宽度。

| 文字角色 | 字号 / 行高 | 字重 |
| --- | --- | --- |
| 元信息、说明文字与紧凑控件 | 13px / 18px | 常规 500 |
| 正文、条目、字段与标准控件 | 15px / 20px | 常规 500 |
| 分组标题 | 16px / 21px | 加粗 700 |
| 页面与内容弹窗标题 | 18px / 24px | 加粗 700 |
| 品牌名 | 22px / 30px | 加粗 700 |

标题、标签和说明文字各自提供新信息，不复述。说明文字限于隐私、安全、不可逆后果、错误、空状态或容易误解的技术含义。

### 间距与尺度

| 角色 | Token | 数值与用途 |
| --- | --- | --- |
| 网页最小宽度 | `--size-page-min-width` | 300px，页面布局维护的最小宽度 |
| 触控目标 | `--size-touch-target` | 44px，仅用于粗指针触屏环境的实际可点击范围 |
| 标题栏 | `--workspace-titlebar-height` | 50px |
| 标题栏按钮 | `--size-titlebar-control` | 30px，纯图标时等宽 |
| 标准条目、标准控件、标准条栏 | `--size-control-primary` | 40px，富内容与多行控件以此为最小高度 |
| 紧凑条目、紧凑控件、紧凑条栏 | `--size-compact-bar` | 30px，用于紧凑的单项信息、单项操作与横向状态栏 |
| 标准内嵌按钮 | `--size-inline-button` | 20px，纯图标时等宽 |
| 紧凑内嵌按钮 | `--size-inline-button-compact` | 18px，纯图标时等宽 |
| 内容间距 | `--space-content` | 15px，内容区内边距、内容分组之间、覆盖层安全距离 |
| 控件水平间距 | `--space-control-inline` | 10px，所有条目、控件与条栏的水平内边距 |
| 标准控件垂直间距 | `--space-control-block` | 10px，标准条目、控件与条栏的垂直内边距 |
| 紧凑控件垂直间距 | `--space-control-block-compact` | 6px，紧凑条目与控件的垂直内边距 |
| 关联间距 | `--space-related` | 5px，标题与内容、内嵌操作内部 |
| 条目间距 | `--space-item` | 2px，同层连续条目；分组列表内部由分隔线组织，不留条目间距 |
| 复选框 | `--size-selection-control` | 16 × 16px，内部对勾 12 × 12px |
| 开关 | `--size-switch-track-width/height` | 32 × 16px，滑块 12 × 12px |

`inline` 表示水平方向，`block` 表示垂直方向。内边距简写按“垂直、水平”引用，`gap` 按“行间距、列间距”分别引用垂直与水平 Token。紧凑条目、控件与条栏统一由共享控件样式管理：内部行高 18px，上下各 6px 内边距，单行总高度 30px；高度 Token 由行高与内边距计算，多行内容自然增高，页面不得另设固定高度或重复叠加内边距。紧凑文字按钮使用 `control--compact`，附件、注释、引用、状态与执行记录条栏使用 `compact-control-bar`。

同一容器只有一层内边距。标准条栏与紧凑条栏都是横向布局，内嵌操作是使用位置，图标按钮是内容形式。标准内嵌按钮用于标准条栏、正文操作和输入辅助操作；紧凑内嵌按钮用于标签、附件和注释等密集内容。标准文字按钮、标题栏按钮、紧凑控件、标准内嵌按钮与紧凑内嵌按钮通过所属尺寸组合外观，不因页面改变。

复合条栏直接组合既有条栏和控件样式：容器使用 `data-row-surface`，主要操作使用 `data-interaction-owner="row"`，附属操作使用 `data-interaction-owner="self"`。悬停、展开与键盘焦点由现有共享规则处理，主要操作禁用时整条撤销悬停，附属操作按自身状态继续可用。条栏承担内边距，内部主要控件不重复增加内边距。附件、注释与报告引用遵守同一规则；纯展示条栏不声明交互表面。

标准条栏由共享样式统一提供上下与左右各 10px 内边距，20px 标准内嵌按钮与 20px 单行内容共同形成 40px 单行高度；40px 为最小高度，多行内容自然增高。页面不得重复声明条栏内边距或固定高度。

粗指针触屏环境将标题栏、标准、紧凑和内嵌控件的实际目标统一提升到至少 44 × 44px；文字和图标尺寸、复选框方框及开关轨道的视觉尺寸保持不变。尺寸扩展通过控件自身留白和列表行高实现，不使用会与相邻控件重叠的透明伪元素。桌面与精细指针环境继续使用上表的紧凑视觉密度。

聊天列表、报告列表与检验指标分类目录的多选状态栏统一组合 `list-selection-heading` 与 `standard-control-bar`，背景透明，透出所在容器的表面，使用主要前景与加粗计数文字，圆角和尺寸继承标准条栏。页面不得另设背景、圆角或文字样式；状态栏不因已选数量或内部按钮交互改变背景，内部按钮仍保留各自的交互反馈。聊天与报告进入多选后在顶部控制区显示计数，底部创建动作隐藏并由批量操作区接替。

用户消息气泡的内边距遵循标准控件规格：上下使用 `--space-control-block`、左右使用 `--space-control-inline`，均为 10px，不使用内容区的 15px 间距。纯文字、包含资源引用及消息编辑状态共用此规则；边框占位不计入内边距。

会话输入框由外容器统一提供标准控件内边距，内部文字输入区域不再叠加内边距。

表面、控件与背景标记使用圆角矩形；圆角为参考高度的四分之一，细长标记以短边为参考。分组列表以所选格式的条目高度为参考，标准格式为 40px、紧凑格式为 30px；根级折叠列表以标准条目为参考；内容弹窗以标题栏为参考；面板使用 48px 参考高度。参考高度不限制实际内容高度。拼接边圆角为零，整体外轮廓由容器裁切。头像、进度、品牌标记及滚动条的专用尺寸见 Token。所有成员头像统一复用账号头像的 30px 尺寸和 7.5px 圆角，内部使用标准用户图标。侧栏健康档案入口不显示分组标题且不可折叠，直接显示当前成员的单行身份栏，并与侧栏底部账号设置共用同一行容器格式：头像在左、加粗成员名称作为标签居中占据剩余宽度，成员名称空间不足时显示省略号；除最右侧“切换”和“添加”控件外，点击身份栏任意位置都打开当前成员的健康档案，最右侧依次放置仅显示 22 × 22px 图标的 30 × 30px“切换”和“添加”按钮。身份栏采用条目与控件的悬停交互归属：主要导航由整条承接，指针进入“切换”或“添加”时整条撤销临时悬停，仅对应控件显示悬停。没有可访问成员时显示占满所在行的标准次要按钮，加号图标与“添加成员”文字紧邻并整体居中，复用标准控件的尺寸、表面、边框与圆角。健康档案工作区顶部再次显示成员身份栏，复用侧栏身份栏的头像、加粗成员名称、表面、间距与圆角；该身份栏不提供切换或添加控件，点击身份栏后在右侧详情栏显示个人信息，不使用弹窗，窄屏进入同一详情层级并通过标题栏返回；身份栏右侧只横向显示已设置的性别值、出生日期值和血型值，以居中点分隔，不显示字段标签。个人信息中的出生日期复用报告日期选择器的纯日期模式，点击其他字段时提交有效日期并关闭选择器，不改变目标字段焦点。身份栏与其后四个功能入口之间使用分隔线，分隔线左右各内缩一个 15px 内容间距，上下分别保留一个内容间距。四个功能入口横向等宽排列，每个入口的图标与名称共同组成一个完整按钮，悬停、键盘焦点和当前选择状态覆盖整个圆角方框。图标占位沿用标题栏控件的 30 × 30px 尺寸，内部图标为 22 × 22px；名称使用 13px / 18px 元信息规格，窄屏保持一排四项。尚未接入内容的入口仍可点按，点按后不改变当前选择或内容。切换成员弹窗继续使用标准成员列表和标准添加控件。

侧栏底部账号设置是单一导航按钮，显示 30 × 30px 账号头像和来自 `account_name` 的加粗账号名称；可用空间不足时显示省略号。导航悬停和当前页状态作用于整条，账号头像自身保持中性表面。

### 图标

图标集中在 `components/icons.tsx`，使用 `50 × 50` viewBox、5 单位内部描边和圆角端点、连接。

显示尺寸：标题栏按钮为 22px；标准条目、标准控件、标准条栏和标准内嵌按钮为 16px；紧凑条目、紧凑控件、紧凑条栏和紧凑内嵌按钮为 14px。

紧凑图标尺寸由共享控件样式统一管理，包含前置图标、内嵌操作和展开箭头。14px 图标在 18px 内容区内垂直居中；页面只定义排列与状态，不再单独设置紧凑图标尺寸。

管理操作区、批量操作区与表单底部操作区统一使用前置图标加文字标签，同一操作在菜单、列表和详情中复用同一图形。置顶使用图钉，收藏使用星形，添加使用加号，删除使用垃圾桶，设置标签使用标签轮廓，合并使用汇合箭头，保存与完成使用对勾。图标继承按钮文字颜色，危险操作沿用危险前景，不另设装饰色；请求期间保留图标与操作位置。空间不足时操作组换行，不缩小图标或挤压文字。日期、筛选选项和普通文本链接不强制附加图标。

关闭、移除使用 `XIcon`，退出登录使用门框与向右箭头组成的 `LogOutIcon`，下拉与折叠使用 `ChevronDownIcon`，进入与返回分别使用右、左箭头。图形本身的圆形线条不影响控件的圆角矩形外观。

## 3. 布局与容器

### 工作区、标题栏与滚动区

标题栏使用等宽左右操作区与居中标题，下缘以分隔线连接内容区。多栏先划分栏位，再复用标题栏；返回表示产品内的信息层级，根级默认保留禁用返回的位置。健康档案根级不显示返回控件，也不在标题栏重复显示切换成员控件；成员切换和添加统一位于侧栏身份栏。

内容分组之间使用内容间距。标准条栏内的操作与状态垂直居中，使用关联间距；附属于正文的操作组不额外套用条栏高度。

全项目使用一套滚动条。滚动区保持左右总留白相等：右侧内容留白加滚动条实际占位等于左侧留白，浮动滚动条不额外占位。固定操作区与滚动正文保持同列对齐，底部空间随固定区域高度调整，不遮挡正文。聊天列表的“发起新聊天”和报告列表的“新增报告”使用与会话输入框相同的底部覆盖关系，悬浮在各自滚动区之上，不随列表滚动；按钮与列表内容同列对齐，滚动区末尾保留至少一个内容间距。

### 分组列表

`GroupedList` 的每次使用都必须显式填写 `density="standard"`（标准格式）或 `density="compact"`（紧凑格式），不提供默认值。格式与字段、导航、选择或操作布局独立组合。同一列表内统一使用所选格式，嵌套列表按自身格式重新确定尺寸。

| 格式 | 单行最小高度 | 字号 / 行高 | 垂直 / 水平内边距 | 图标 | 内嵌图标按钮 | 列表与交互条目圆角 |
| --- | --- | --- | --- | --- | --- | --- |
| 标准 | 40px | 15px / 20px | 10px / 10px | 16px | 20px | 10px |
| 紧凑 | 30px | 13px / 18px | 6px / 10px | 14px | 18px | 7.5px |

标准格式用于常规浏览与编辑，紧凑格式用于密集信息。共享列表样式统一控制条目、字段输入、图标和内嵌操作的尺寸；页面只选择格式与布局。复选框和开关保留各自专用尺寸，多行条目随内容自然增高。

`GroupedList` 提供一个无外框、无投影的控件表面，两种格式的相邻条目都共享内部分隔线。分隔线两端各内缩控件间距。静态条目透明，交互时只突出对应条目并隐藏相邻分隔线。首项、中间项和末项的交互表面均为完整四角圆角，不依赖容器裁切；整行内部按钮继承条目轮廓，页面不得另设圆角或交互表面。展开容器的标题与内容拼接边仍遵循容器规则。

列表内容可为单个或多个条目；字段、导航和只读内容不另设表面。富内容条目自然增高，上下保留控件间距。列表内部的创建入口存在时固定为第一项；聊天与报告的工作区级创建动作例外，固定悬浮在所属列表滚动区底部，列表末端预留其高度与内容间距。非空列表的计数位于列表之后，不包含创建入口，零数量不单独显示。

根级折叠列表是可折叠的列表容器，各组独立，组间使用内容间距。标题使用标题表面，展开时显示下缘分隔线。展开状态、可访问名称和方向箭头保持一致，内容复用列表条目。数量、滚动上限和默认展开状态由页面契约决定。

### 数据表、正文与面板

跨列比较的同构记录使用数据表，不强制改成字段条目。连续阅读使用正文；面板用于通知等非列表独立内容，不在列表外再包面板。媒体使用透明预览舞台，不附加装饰性表面。

### 覆盖层

覆盖层挂载到页面顶层，不受工作区裁切。选择菜单的宽度与水平对齐分别由使用场景明确指定，单选和多选共用以下规则：

- 独立选择框、报告类型筛选与检验指标分类筛选：菜单与实际触发控件等宽，左右边缘对齐，每个选项的可点击区域覆盖菜单整行。
- 字段行右侧的值选择：菜单宽度由最长标签及必要的图标、复选框与内边距决定，右边缘与实际选择控件对齐，向左延展。整行高亮只表达操作状态，不决定菜单宽度或定位锚点。
- 紧凑菜单：按内容定宽，左侧入口左边缘对齐，右侧入口右边缘对齐。紧凑成员选择与授权权限选择使用右边缘对齐。

选择菜单锚定实际触发控件，默认向下展开，下方空间不足时向上展开。菜单最大宽度与高度受可视区域安全距离约束，水平位置在所选对齐基础上避让边缘，过长内容在菜单内部滚动。滚动、窗口缩放、触发控件或菜单内容尺寸变化后重新定位；可用空间恢复时，菜单恢复所选规则下的宽度。

共享单选、多选组件通过必填的 `menuWidth="trigger|content"` 与 `menuAlign="start|end"` 分别指定宽度与对齐，不根据类名、单选或多选类型、交互高亮归属推断。上下文菜单按内容定宽，根据触发位置定位，并遵守相同的可视区域安全距离。

日期与时间选择器共用同一覆盖层外观，纯日期模式用于出生日期，日期与时间模式用于报告时间。年月标题使用分组标题文字与透明背景，两侧切月按钮复用标题栏图标控件；年月日输入按列对齐，日期与时间输入均不显示字段标签文字，通过无障碍名称标识用途；日历复用标准控件与单选状态，今天额外使用短下划线标记。底部“今天”或“现在”按内容定宽，取消与完成位于右侧并复用标准按钮及其图标。覆盖层使用统一边框、圆角、阴影和安全距离，日历列随可用宽度伸缩，内容超出高度时在层内滚动。

内容弹窗相对可视区域居中，复用标题栏、正文与可选底部操作区；正文和操作区使用内容间距。内容弹窗在窄屏继续居中，两侧和上下各保留至少一个 15px 内容间距；宽度受遮罩可用宽度约束，高度使用动态可视区域限制，正文在弹窗内滚动，标题栏和底部操作区保持可达。长标题单行省略，关闭控件保留完整尺寸；窄屏底部操作按钮纵向排列。媒体预览在窄屏可占满动态可视区域，标题和下载、关闭控件分别占用可收缩列与操作列。只有覆盖层使用 `--shadow-overlay`，页面流内不使用投影。

弹窗的相邻区域按最终可见距离计算留白，不叠加各自的内边距。标题栏下缘到正文为 15px；正文末项到操作按钮为 15px，操作按钮到弹窗底部内缘也为 15px，边框占位另计。正文和底部操作区作为相邻区域排列，不把操作区嵌入已带内边距的正文。正文保留底部内容间距，紧接其后的操作区不再增加顶部内边距；没有底部操作区时，正文独自保留底部 15px。正文发生滚动时，在滚动末端验证末项到按钮或底边的间距。水平留白同样只保留一层内容间距，正文与操作区保持同列对齐。

选区上下文操作浮层是覆盖层中的单一动作控件，不是提示气泡、菜单或新的按钮外观。用户在可注释正文中选取有效文本时，它固定定位于选区上方，并在视口左右边缘留出安全距离。控件本体使用 `--color-control-surface`、`--color-foreground` 和 `--color-divider`，采用标准控件高度、圆角、图标尺寸、内边距与 `--layer-menu`；鼠标悬停、键盘焦点和禁用状态完全复用标准控件交互规则。浮层随选区失效、添加成功、外部操作或 Escape 消失，不保留反色专用表面、边框或交互状态。

## 4. 条目与控件

### 条目布局

- 字段：列表共享标签列与值列，标签列取组内最长标签宽度，值列占满剩余空间。标签左对齐，紧凑值右对齐；可编辑单行内容内部横向滚动，只读紧凑值省略。
- 长文本字段：设置类说明使用元信息规格，单行右对齐、多行左对齐并自动增高；连续阅读的结构化报告事实使用正文规格、左对齐与自然换行。
- 导航：标签、当前摘要、右箭头共享列轨。摘要使用元信息规格，单行右对齐、多行左对齐，箭头不被长文本挤出。
- 选择：复选框位于文字左侧；单选条目通过选择状态表达当前值，不显示复选框。
- 操作：一项只承担一个主要意图；列表内操作继承条目尺寸与状态，不嵌入另一层按钮表面。创建入口的图标与文字紧邻、整体居中，不复用导航条目的分列布局。

只读值使用静态信息项，编辑使用真实输入控件并正确关联标签。局部校验操作位于相关列表末项，表单提交位于列表外。敏感文本统一掩码并使用可见性按钮。

### 控件组合

按钮分别选择尺寸、外观和语义：主要操作使用杏橙实色表面与深棕前景，次要操作使用控件表面，标准与紧凑内嵌图标操作默认透明；危险语义改变配色，不改变尺寸。选区上下文操作浮层复用次要操作外观，不新增反色按钮变体。同一操作组使用相同文字与尺寸。独立危险按钮默认使用危险表面，悬停改用危险操作悬停表面；菜单内危险操作默认仅显示危险前景，悬停时使用同一危险操作悬停表面。危险按钮在默认、悬停与禁用状态均不绘制边框，键盘导航时仍显示全局焦点轮廓。

输入、单选、多选和密钥控件共享控件尺寸与焦点规则；位于字段条目内时使用透明表面，不重复绘制内层边框。交互归属不按 HTML 标签判断，而按意图判断：主要意图由条目承接，独立意图由控件自身承接。原生可编辑输入由字段或输入表面直接承接；嵌套触发器使用 `data-interaction-owner="row|self"` 明确归属。字段选择、整行选择和主要导航使用 `row`，筛选器、工具栏及同一行中的独立操作使用 `self`。独立下拉控件由触发器承接。单选完成后关闭菜单，多选保持展开。

复选框表达可编辑多选，开关表达开与关。未勾选复选框的边框、关闭状态开关的边框与滑块使用次要前景色；开启状态使用深橙轨道与浅色滑块，使控件轮廓和位置始终清晰可辨。成功、失败、完成、复制成功和连接状态使用状态图标或文字，不借用复选框、开关表达结果。

### 分档滑块

分档滑块使用原生范围输入，只允许选择当前对象支持的离散档位；刻度按档位等距排列，当前值以文字显示在左侧。模型选择与分档滑块组合时，上方直接显示可点击的模型名称，两行复用标准 `GroupedList`，模型名称与当前等级均为 15px / 20px、常规 500，不重复显示字段标签；无障碍名称仍保留用途。

滑块轨道高 16px、圆角 4px，拖动块为 20 × 20px、圆角 5px，刻度为 4 × 4px、圆角 1px，分别由 `--size-slider-*` 与 `--radius-slider-*` Token 管理。全部采用短边四分之一的圆角矩形，不使用圆形拖动块、胶囊轨道或投影。轨道以分隔线色承载未选区、强调色承载已选区，拖动块使用主表面与 1px 次要前景边框；填充仅表达数值进度，不使用装饰性渐变。原生范围输入的轨道保持透明，由共享样式统一绘制完整四边轮廓，避免浏览器默认轨道遮住边框。

共享 `.discrete-slider` 管理控件外观，标准字段条目承接悬停、实际输入焦点与键盘轮廓：悬停浅色、聚焦编辑深色、四角完整圆角，高亮时隐藏相邻缩进分隔线，退出后恢复。粗指针环境扩大实际操作目标到至少 44px，轨道、刻度与拖动块的视觉尺寸不变。支持拖动、点击轨道、方向键逐档调整及 Home / End 选择两端；用 `aria-valuetext` 提供档位名称。调整时保持弹层打开；仅有一个可用档位时禁用滑块并继续显示当前值。

## 5. 状态与交互

### 状态职责

| 状态 | 表达与边界 |
| --- | --- |
| 默认 | 条目继承容器表面，控件使用所属外观 |
| 鼠标悬停 | 可用、未选中、未编辑的控件与只读条目使用浅色 `--color-item-hover`；条目正文、空白区域及 `row` 主要控件由整个条目承接悬停；指针进入 `self` 附属操作时条目撤销临时悬停，仅该操作变浅；控件本身就是条目时由该控件承接完整条目表面；移开且未打开上下文菜单时恢复，不产生光圈 |
| 正在编辑 | 字段整行、独立表单输入框自身、会话输入表面及用户消息编辑表面使用深色 `--color-item-current`，实际可编辑输入失焦或结束编辑后撤销；次级表面只规定默认底色，不取消悬停或编辑状态 |
| 当前页面、单选、已开启的状态按钮 | 持续使用深色；悬停不能覆盖 |
| 下拉菜单展开 | 字段中的选择控件由整行临时变深，独立选择控件由触发器变深；关闭恢复，已有值不触发深色 |
| 上下文菜单打开 | 触发条目整行保持浅色 `--color-item-hover`，鼠标移入菜单或离开条目后仍保留完整圆角与相邻分隔线隐藏；已有当前对象、单选或编辑深色优先。菜单关闭后恢复条目的实际状态 |
| 键盘导航焦点 | 在已有状态色上叠加清晰的 2px 强调色轮廓，不改变底色；鼠标点击后取消轮廓 |
| 多选 | 仅复选框表达持久勾选状态；条目不因勾选改变表面或分隔线，悬停可变浅，键盘导航只叠加轮廓。进入批量选择时不沿用浏览中的当前对象高亮 |
| 禁用、运行 | 不响应悬停；保留操作位置、原有状态含义与语义色相，弱化显示并禁止重复提交 |
| 成功、失败 | 优先通过值、列表、按钮文字或图标反馈；需解释时使用右上角通知 |

当前对象、单选、编辑与展开不是同一业务状态。编辑允许复用深色，但不能给编辑字段伪加 `aria-current`；多选不能借用当前对象表面。普通按钮不会因为点击过而持续变深，折叠标题不会因为展开而成为当前对象。瞬时结果不保存为当前状态；可重复操作只在请求中禁用，结束后恢复。

判断示例：字段下拉与行内日期选择用于修改该字段的唯一值，归属 `row`；侧栏聊天、报告条目和模型提供方的主要选择动作代表整行意图，也归属 `row`。筛选下拉只改变工具栏条件，归属 `self`。执行记录的披露标题是独立的展开控件，归属 `self`，只改变标题表面，不把正文容器视为可点击条目。授权创建行以档案勾选作为主要意图，勾选控件归属 `row`，同行的权限选择归属 `self`；勾选后的持续状态仍只由复选框表达。已授权行没有整行主操作，静态档案与接收方身份不产生悬停，权限选择和撤销分别归属 `self`。等待队列、收藏标签等复合条目中的拖动、编辑、删除、显隐与移除也各自归属 `self`；不得因控件都是按钮或都位于同一行而套用同一种悬停表面。

上下文菜单的触发条目高亮随菜单存续，右击、长按和键盘打开遵守同一规则，不改变当前对象或多选勾选。切换到另一条目的菜单时，高亮随触发条目转移；执行操作、取消、外部点击、来源祖先滚动或导航关闭菜单后撤销临时高亮，鼠标仍悬停时保留正常悬停颜色。

### 编辑归属与优先级

- 字段条目是唯一的整行底色与字段主要控件焦点轮廓拥有者，内部输入和 `row` 选择触发器透明。独立表单输入框拥有自身底色。文本、密码、数值、日期时间、多行、搜索与报告正文编辑遵守同一规则；会话重命名属于侧栏条目内编辑，由整行承接，输入框不得另画边框。
- 会话输入框的外容器连同工具栏，以及用户消息气泡，默认使用次级表面 `--color-rail`。指针位于文字输入区域时外容器变为浅色，实际可编辑输入聚焦时外容器变为深色；内部文本输入始终透明。附件、模型、用量、发送和消息编辑操作归属 `self`，分别显示自身反馈；它们获得焦点后外容器恢复其实际状态。若文字输入仍保持焦点，仅把指针移到附属操作不会覆盖深色持续状态。附件与引用链接显示浅色或深色状态表面时，同步采用正文颜色保证可读性。
- 编辑只取实际可编辑输入焦点，不取已有值、未保存草稿、保存请求状态或页面编辑模式属性。焦点从“用户标识”转到“账号名称”时深色跟随移动；失焦后即使保存失败或仍展示编辑器，也不能保留编辑底色。若鼠标仍在原字段上，恢复为浅色悬停。
- 只读内容、复选框、文件输入和下拉触发器不算文字编辑。内嵌显隐、清除等按钮拥有自己的悬停、状态色和轮廓，不能借其键盘焦点让字段整行变深。
- 悬停规则使用低优先级；编辑、当前对象、单选、已开启状态和展开触发器的深色优先，键盘轮廓独立叠加。多选容器明确声明 `selectionMode="multiple"` 或 `aria-multiselectable="true"`，通用按下状态不能染深复选条目。
- 主操作、危险按钮、复选框及开关本体保留现有语义配色，只统一交互优先级。列表分隔线不能穿过高亮或焦点轮廓；取消高亮后恢复原分隔线。

### 键盘与可访问性

图标按钮提供 `aria-label`，装饰图标使用 `aria-hidden="true"`；状态不能只依赖颜色。所有交互必须支持键盘，焦点移动不引起无关滚动。

应用统一跟踪输入方式。鼠标点击取消轮廓，Tab、方向导航、键盘确认或取消引起的焦点转移显示轮廓；普通打字、输入法组合和鼠标移动不切换输入方式。不能直接以原生 `:focus-visible` 判断输入方式，因为鼠标点入文本框也可能匹配它。菜单、弹窗的初始聚焦与焦点归还沿用打开或关闭动作的输入方式。

同一焦点只绘制一层轮廓：分组条目内描边，独立控件外描边。`row` 控件聚焦时标记最近的字段条目、行表面或分组条目；`self` 控件聚焦时只标记自身。字段输入或选择触发器聚焦时标记整行；显隐、移除等内嵌操作按钮聚焦时只标记按钮，不同时标记行和输入框。键盘轮廓不替代当前状态色，也不额外增加悬停底色。

所有 `aria-modal="true"` 的正式模态使用统一焦点契约：打开时背景工作区不可交互；初始焦点落在标题，需立即输入时可落在主要输入。Tab 与 Shift+Tab 在模态及其顶层选择菜单内循环，Escape 优先关闭内部菜单，操作未锁定时关闭或返回模态。关闭后归还来源焦点；来源已移除时不强行聚焦失效节点。

### 响应式与反馈文案

断点只改变栏位、排列、显隐、换行、滚动和覆盖层位置，不改变同一组件的字号、行高、尺寸、圆角、字重及状态含义。弹窗、菜单、长文本与数据表不得超出可视区域。启用减少动态效果偏好时停止非必要动画，保留静态状态线索。

可创建的空列表使用创建入口；其他空状态使用简短单行正文。独立空列表、筛选无结果和未选择详情等占据剩余内容区的提示上下左右居中，局部空状态只水平居中。计数的量词与对象一致，不追加重复说明。

## 6. 实现与维护

- Token 保存数值和语义角色；元素重置、基础继承、滚动条及全局可访问性归入基础样式。
- 共享设计样式负责容器、条目、控件与状态；内容样式负责 Markdown 等内容渲染；页面样式只负责布局、内容适配和明确的业务表现。
- `interaction-states.css` 集中管理状态组合，`interactionFocus.ts` 管输入方式与唯一轮廓归属。页面不得覆盖悬停、编辑和焦点配色；移除被替代的规则，不叠加高优先级补丁。
- 新规则先确定所属层级和现有角色。两个页面需要同一修正时提升到共享层并删除局部重复，不增加同义类名、兼容别名或提高选择器权重的补丁。
- 调整共享规则时同步所有调用点和相关测试，以实际布局、状态及交互验收，不要求保留已替代的实现结构。
- 浏览器验收优先操作真实页面和真实组件。只有滚动、动态尺寸等必须依赖真实 DOM 几何的行为才使用最小测试页面；只有实现存在断点或浏览器分支时才扩展视口、Chromium 或 WebKit 矩阵。测试验证用户可观察结果，不以源码字符串、CSS 选择器文本或重复像素常量代替行为，也不修改真实业务数据。
- 前端行为文档只引用此处角色；技术契约维护接口与持久化，README 只提供入口与开发说明。未来愿景不参与当前执行。
